<!DOCTYPE html>
<html lang="zh-TW">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
<meta name="apple-mobile-web-app-title" content="Coach Claude">
<meta name="theme-color" content="#e8eef8">
<title>Coach Claude</title>
<link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700&family=DM+Mono:wght@400;500&family=Noto+Sans+TC:wght@300;400;500&display=swap" rel="stylesheet">
<style>
:root{
  --b1:#7aaad8;--b2:#9888d0;--b3:#c8a8e0;--b4:#d8b8e8;
  --rose:#c8a0c0;--silver:#c8d8e8;
  --bg:#daeaf8;
  --card:rgba(240,246,255,0.72);
  --border:rgba(180,200,240,0.4);
  --text:#081428;--text-m:rgba(26,36,56,0.52);--text-d:rgba(26,36,56,0.30);
  --grad:linear-gradient(135deg,#0842c8,#0870d0,#0858c8);
  --mono:'DM Mono',monospace;
  --body:'Outfit','Noto Sans TC',sans-serif;
  --safe:env(safe-area-inset-bottom,0px);
}
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
html,body{height:100%;overflow:hidden}
body{background:var(--bg);color:var(--text);font-family:var(--body);-webkit-font-smoothing:antialiased;overscroll-behavior:none}
::-webkit-scrollbar{width:0}
input,textarea,select{font-family:inherit;-webkit-appearance:none}

/* ══ IRIDESCENT MESH BG ══════════════════════════════════════ */
.mesh{position:fixed;inset:0;z-index:0;pointer-events:none;overflow:hidden}
.mesh::before{
  content:'';position:absolute;inset:-15%;
  background:
    radial-gradient(ellipse 75% 65% at 8%   2%,  rgba(120,160,220,0.70) 0%,rgba(160,200,240,0.35) 35%,transparent 60%),
    radial-gradient(ellipse 65% 75% at 95%  8%,  rgba(170,140,220,0.58) 0%,rgba(200,170,240,0.28) 32%,transparent 56%),
    radial-gradient(ellipse 60% 55% at 90%  92%, rgba(210,170,225,0.52) 0%,rgba(230,195,240,0.25) 33%,transparent 55%),
    radial-gradient(ellipse 58% 65% at 4%   92%, rgba(140,180,230,0.45) 0%,rgba(170,200,240,0.22) 32%,transparent 54%),
    radial-gradient(ellipse 48% 42% at 50%  50%, rgba(235,230,250,0.65) 0%,rgba(245,240,255,0.38) 30%,transparent 52%),
    radial-gradient(ellipse 30% 25% at 28%  22%, rgba(255,255,255,0.52) 0%,transparent 48%),
    radial-gradient(ellipse 25% 20% at 72%  78%, rgba(255,240,255,0.38) 0%,transparent 45%),
    #e4ecf6;
  animation:meshpulse 20s ease-in-out infinite alternate;
}
/* Iridescent film layer — the magic */
.mesh::after{
  content:'';position:absolute;inset:0;
  background:
    radial-gradient(ellipse 30% 22% at 20% 18%, rgba(255,255,255,0.62) 0%,transparent 50%),
    radial-gradient(ellipse 22% 16% at 82% 68%, rgba(200,230,255,0.48) 0%,transparent 46%),
    radial-gradient(ellipse 18% 14% at 55% 8%,  rgba(255,255,255,0.55) 0%,transparent 46%);
  animation:iridescent 11s ease-in-out infinite alternate;
  mix-blend-mode:screen;
}
@keyframes meshpulse{
  0%  {transform:scale(1) rotate(0deg)}
  40% {transform:scale(1.04) rotate(0.5deg)}
  100%{transform:scale(1.01) rotate(-0.3deg)}
}
@keyframes iridescent{
  0%  {opacity:.7;transform:translate(0,0) scale(1)}
  30% {opacity:1; transform:translate(3%,-2%) scale(1.02)}
  70% {opacity:.85;transform:translate(-2%,2%) scale(0.99)}
  100%{opacity:.9;transform:translate(1%,-1%) scale(1.01)}
}

/* Sparkle stars */
.stars{position:fixed;inset:0;z-index:0;pointer-events:none}
.star{position:absolute;border-radius:50%;animation:twinkle var(--d) ease-in-out infinite var(--dl)}
@keyframes twinkle{0%,100%{opacity:0;transform:scale(0.4)}50%{opacity:var(--op);transform:scale(1)}}

/* ══ LAYOUT ══════════════════════════════════════════════════ */
#app{display:flex;flex-direction:column;height:100vh;height:100dvh;max-width:480px;margin:0 auto;position:relative;overflow:hidden}

#hdr{
  flex-shrink:0;padding:14px 20px 12px;
  padding-top:calc(14px + env(safe-area-inset-top,0px));
  background:rgba(210,232,252,0.82);
  backdrop-filter:blur(32px) saturate(200%);
  -webkit-backdrop-filter:blur(32px) saturate(200%);
  border-bottom:1px solid rgba(255,255,255,0.75);
  z-index:50;display:flex;align-items:center;justify-content:space-between;position:relative;
  box-shadow:0 1px 0 rgba(255,255,255,0.95) inset,0 2px 20px rgba(100,130,200,0.1);
}
.logo-sub{font-size:8px;letter-spacing:5px;color:var(--text-d);text-transform:uppercase;font-family:var(--mono);margin-bottom:2px}
.logo-main{font-size:24px;font-weight:700;letter-spacing:-0.5px;background:var(--grad);-webkit-background-clip:text;-webkit-text-fill-color:transparent;line-height:1.1}
.sdot{width:6px;height:6px;border-radius:50%;background:var(--b1);box-shadow:0 0 8px rgba(122,170,216,0.8),0 0 18px rgba(152,136,208,0.4);animation:gpulse 2s infinite}
.stxt{font-size:8px;color:var(--b1);font-family:var(--mono);letter-spacing:2px}

#content{flex:1;overflow-y:auto;overflow-x:hidden;position:relative;z-index:1;-webkit-overflow-scrolling:touch}
.page{padding:16px 14px;padding-bottom:calc(90px + var(--safe));display:none;animation:fadeUp .3s ease}
.page.active{display:block}

#nav{
  flex-shrink:0;display:flex;justify-content:space-around;
  padding:10px 8px;padding-bottom:calc(10px + var(--safe));
  background:rgba(210,232,252,0.88);
  backdrop-filter:blur(32px) saturate(200%);
  -webkit-backdrop-filter:blur(32px) saturate(200%);
  border-top:1px solid rgba(255,255,255,0.75);
  z-index:50;position:relative;
  box-shadow:0 -1px 0 rgba(255,255,255,0.95) inset;
}
.nb{background:none;border:none;cursor:pointer;display:flex;flex-direction:column;align-items:center;gap:3px;padding:4px 10px;border-radius:12px}
.nb .ni{font-size:18px}
.nb .nl{font-size:8px;font-family:var(--mono);letter-spacing:1px;color:var(--text-d);transition:color .2s}
.nb.active .nl{color:#5880b0}
.nb .nbar{width:0;height:1.5px;border-radius:1px;background:var(--grad);transition:width .3s}
.nb.active .nbar{width:16px}

/* ══ PEARL CARDS ════════════════════════════════════════════ */
.card{
  background:linear-gradient(145deg,rgba(240,250,255,0.90),rgba(215,238,255,0.84),rgba(228,245,255,0.87),rgba(208,235,255,0.90));
  border:1px solid rgba(255,255,255,0.82);
  border-radius:20px;padding:16px;margin-bottom:12px;
  backdrop-filter:blur(24px) saturate(160%);
  -webkit-backdrop-filter:blur(24px) saturate(160%);
  position:relative;overflow:hidden;
  box-shadow:
    -4px -4px 10px rgba(255,255,255,0.88),
    5px  5px  14px rgba(80,140,210,0.38),
    0 1px 0 rgba(255,255,255,0.98) inset,
    0 -1px 0 rgba(80,150,220,0.14) inset,
    0 0 0 0.5px rgba(120,180,240,0.42);
}
/* Iridescent pearl sheen on cards */
.card::before{
  content:'';position:absolute;top:0;left:0;right:0;height:40%;
  background:linear-gradient(180deg,rgba(255,255,255,0.55) 0%,rgba(220,232,255,0.15) 50%,transparent 100%);
  border-radius:20px 20px 0 0;pointer-events:none;
}
/* Color shift shimmer */
.card::after{
  content:'';position:absolute;inset:0;
  background:linear-gradient(125deg,rgba(200,215,255,0.18) 0%,rgba(220,200,240,0.12) 40%,rgba(255,220,240,0.08) 70%,transparent 100%);
  border-radius:20px;pointer-events:none;
  animation:cardsheen 8s ease-in-out infinite alternate;
}
@keyframes cardsheen{
  0%  {opacity:.5}
  50% {opacity:.85}
  100%{opacity:.55}
}
.card.pglow{
  border-color:rgba(80,160,240,0.45);
  box-shadow:0 6px 30px rgba(60,140,220,0.18),0 1px 0 rgba(255,255,255,0.98) inset;
}
.card.iglow{
  border-color:rgba(80,165,245,0.42);
  box-shadow:0 6px 30px rgba(60,145,225,0.16),0 1px 0 rgba(255,255,255,0.98) inset;
}
.card.spark{
  background:linear-gradient(135deg,rgba(210,225,250,0.62),rgba(225,210,245,0.52),rgba(245,220,240,0.45));
  border-color:rgba(220,210,245,0.55);
}
.card.orbit{background:rgba(200,215,245,0.28);border-color:rgba(190,205,235,0.42)}

/* ══ SECTION LABELS ══════════════════════════════════════════ */
.slabel{font-size:8px;letter-spacing:4px;text-transform:uppercase;font-family:var(--mono);display:flex;align-items:center;gap:8px;margin-bottom:12px;font-weight:500}
.slabel::after{content:'';flex:1;height:1px;background:linear-gradient(90deg,rgba(120,160,220,0.3),rgba(180,150,220,0.2),transparent);border-radius:1px}
.si{color:#0838c0}.sv{color:#0858c0}.sk{color:#0868d0}.sb{color:#0850c8}

/* ══ PROGRESS ════════════════════════════════════════════════ */
.ptrack{height:4px;background:rgba(8,100,220,0.14);border-radius:99px;overflow:hidden}
.pfill{height:100%;border-radius:99px;transition:width 1.4s cubic-bezier(0.22,1,0.36,1)}

/* ══ JOURNEY RINGS ═══════════════════════════════════════════ */
.journey-grid{display:grid;grid-template-columns:1fr 32px 1fr;gap:0;align-items:center;margin-bottom:12px}
.jpanel{
  border-radius:20px;padding:14px 10px 12px;
  border:1px solid rgba(255,255,255,0.82);
  background:rgba(242,248,255,0.68);
  backdrop-filter:blur(20px);-webkit-backdrop-filter:blur(20px);
  box-shadow:0 4px 20px rgba(100,130,200,0.1),0 1px 0 rgba(255,255,255,0.98) inset;
  position:relative;overflow:hidden;
}
.jpanel::before{content:'';position:absolute;top:0;left:5%;right:5%;height:1.5px;background:linear-gradient(90deg,transparent,rgba(255,255,255,1),transparent)}
.jpanel::after{content:'';position:absolute;inset:0;background:linear-gradient(135deg,rgba(200,218,255,0.12),rgba(220,200,240,0.08),rgba(255,215,235,0.06));border-radius:20px;pointer-events:none}
.jpanel.goal{
  border-color:rgba(190,175,240,0.5);
  background:linear-gradient(155deg,rgba(215,208,252,0.60),rgba(205,222,252,0.50),rgba(240,215,248,0.45));
  box-shadow:0 8px 34px rgba(150,130,220,0.22),0 1px 0 rgba(255,255,255,0.98) inset;
}
.jlabel{font-size:7px;letter-spacing:4px;font-weight:600;text-transform:uppercase;font-family:var(--mono);text-align:center;margin-bottom:10px}
.jlabel.now{color:#4870a8}.jlabel.goal{color:#6050a0}
.ring-wrap{display:flex;justify-content:center;margin-bottom:10px;position:relative}
.jbadges{display:flex;gap:3px;justify-content:center;margin-bottom:10px;flex-wrap:wrap}
.jbadge{font-size:7px;font-weight:600;padding:2px 8px;border-radius:20px;font-family:var(--mono);letter-spacing:0.5px}
.bi{background:rgba(100,148,210,0.13);color:#3868a0;border:1px solid rgba(100,148,210,0.26)}
.bv{background:rgba(140,120,205,0.13);color:#5848a0;border:1px solid rgba(140,120,205,0.26)}
.bk{background:rgba(180,110,175,0.11);color:#884088;border:1px solid rgba(180,110,175,0.22)}
.by{background:rgba(180,152,60,0.10);color:#806820;border:1px solid rgba(180,152,60,0.18)}
.jstats{display:flex;flex-direction:column;gap:5px}
.jstat{display:flex;align-items:center;padding:4px 6px;border-radius:8px;background:rgba(140,175,230,0.09)}
.jsdot{width:6px;height:6px;border-radius:50%;flex-shrink:0;margin-right:6px}
.jslabel{font-size:9px;color:rgba(8,25,65,0.62);flex:1;font-family:var(--mono)}
.jsval{font-size:11px;font-weight:600;font-family:var(--mono)}
.vi{color:#3868a0}.vv{color:#5848a0}.vk{color:#884088}.vb{color:#0848b8}
.mbar{height:2px;background:rgba(140,175,230,0.12);border-radius:99px;overflow:hidden;margin-top:2px}
.mfill{height:100%;border-radius:99px;transition:width 1.2s cubic-bezier(0.22,1,0.36,1)}
.arrow-mid{display:flex;align-items:center;justify-content:center}
.arrow-c{width:26px;height:26px;border-radius:50%;background:var(--grad);display:flex;align-items:center;justify-content:center;font-size:11px;color:#fff;box-shadow:0 4px 16px rgba(120,140,210,0.45),0 0 0 1px rgba(255,255,255,0.4);font-family:var(--mono)}

/* ══ PEARL CHROME RING ANIMATION ════════════════════════════ */
@keyframes pearlspin{from{transform:rotate(0deg)}to{transform:rotate(360deg)}}
@keyframes pearlspin2{from{transform:rotate(0deg)}to{transform:rotate(-360deg)}}
@keyframes pearlspin3{from{transform:rotate(0deg)}to{transform:rotate(360deg)}}
.ring-spin1{animation:pearlspin  12s linear infinite;transform-origin:59px 59px}
.ring-spin2{animation:pearlspin2 16s linear infinite;transform-origin:59px 59px}
.ring-spin3{animation:pearlspin3 20s linear infinite;transform-origin:59px 59px}

@keyframes goalpulse{
  0%,100%{filter:drop-shadow(0 0 8px rgba(140,160,230,0.55)) drop-shadow(0 0 18px rgba(180,150,230,0.28))}
  50%{filter:drop-shadow(0 0 16px rgba(200,170,240,0.70)) drop-shadow(0 0 30px rgba(140,180,240,0.38))}
}
.goal-svg{animation:goalpulse 4s ease-in-out infinite}
.jdate{text-align:center;font-size:7px;color:var(--text-d);margin-top:4px;letter-spacing:2px;font-family:var(--mono)}

/* ══ RACE ITEMS ══════════════════════════════════════════════ */
.race-item{display:flex;align-items:center;gap:10px;padding:10px 12px;background:rgba(235,245,255,0.60);border-radius:14px;border:1px solid rgba(255,255,255,0.80);margin-bottom:8px;cursor:pointer;transition:all .2s;box-shadow:0 2px 10px rgba(100,130,200,0.08)}
.race-item:active{transform:scale(0.98);background:rgba(235,245,255,0.80)}
.ri-icon{font-size:18px}.ri-info{flex:1}
.ri-name{font-size:12px;color:#081428;font-weight:600}
.ri-target{font-size:9px;color:rgba(8,25,65,0.52);margin-top:1px;font-family:var(--mono)}
.ri-num{font-family:var(--mono);font-size:20px;font-weight:700;background:var(--grad);-webkit-background-clip:text;-webkit-text-fill-color:transparent;line-height:1}
.ri-unit{font-size:8px;color:var(--text-d);letter-spacing:1px;font-family:var(--mono)}
.ri-chev{font-size:11px;color:var(--text-d);margin-left:2px}

/* ══ ORBIT ════════════════════════════════════════════════════ */
.orbit-hdr{display:flex;align-items:center;justify-content:space-between;cursor:pointer;user-select:none}
.orbit-chev{font-size:12px;color:#7060a8;transition:transform .3s}
.orbit-chev.open{transform:rotate(180deg)}
.orbit-body{overflow:hidden;transition:max-height .4s cubic-bezier(0.22,1,0.36,1),opacity .3s;max-height:0;opacity:0}
.orbit-body.open{max-height:600px;opacity:1}
.orbit-sec{margin-bottom:10px}
.orbit-sec-label{font-size:8px;color:#7060a8;letter-spacing:3px;margin-bottom:4px;font-weight:600;font-family:var(--mono)}
.orbit-sec-text{font-size:12px;color:rgba(8,25,65,0.70);line-height:1.78;font-weight:400}
.orbit-action{margin-top:10px;padding:10px 12px;background:rgba(150,130,210,0.09);border-radius:12px;border-left:2px solid #9878c8}
.orbit-act-label{font-size:8px;color:#7060a8;letter-spacing:3px;margin-bottom:4px;font-family:var(--mono)}
.orbit-act-text{font-size:12px;color:#081428;font-weight:500}

/* ══ SPARK ════════════════════════════════════════════════════ */
.spark-q{font-size:14px;color:#081428;line-height:1.82;margin-top:10px;font-style:italic;font-weight:300}
.spark-src{font-size:9px;color:rgba(8,25,65,0.52);margin-top:5px;font-family:var(--mono);letter-spacing:1px}

/* ══ COACH AVATAR ════════════════════════════════════════════ */
.cavatar{width:36px;height:36px;border-radius:50%;background:linear-gradient(135deg,#0842c8,#0870d0);display:flex;align-items:center;justify-content:center;font-size:17px;flex-shrink:0;box-shadow:0 4px 16px rgba(120,140,210,0.38),0 0 0 1.5px rgba(255,255,255,0.6)}

/* ══ RACE DETAIL ══════════════════════════════════════════════ */
.back-btn{background:rgba(120,160,220,0.10);border:1px solid rgba(120,160,220,0.26);border-radius:10px;padding:6px 12px;font-size:12px;color:#0842c8;cursor:pointer;font-family:inherit;font-weight:600}
.rstat{text-align:center;padding:8px 4px;background:rgba(150,180,230,0.10);border-radius:10px}
.rstat-val{font-size:15px;font-weight:700;color:#0842c8;font-family:var(--mono)}
.rstat-label{font-size:8px;color:var(--text-d);margin-top:2px;font-family:var(--mono);letter-spacing:1px}
.training-item{display:flex;align-items:center;gap:10px;padding:9px 12px;background:rgba(235,245,255,0.60);border:1px solid rgba(255,255,255,0.80);border-radius:12px;margin-bottom:6px;box-shadow:0 1px 6px rgba(100,130,200,0.07)}
.training-name{font-size:12px;color:#081428;font-weight:500;flex:1}
.training-tag{font-size:8px;padding:2px 8px;border-radius:20px;font-weight:600;font-family:var(--mono)}
.hyrox-tag{background:rgba(255,230,0,0.15);color:#7a6000}
.running-tag{background:rgba(100,148,210,0.13);color:#3868a0}
.duathlon-tag{background:rgba(140,120,205,0.13);color:#5848a0}
.btn-add-record{width:100%;background:rgba(235,245,255,0.55);border:1px dashed rgba(150,180,230,0.38);border-radius:12px;padding:11px;color:var(--text-d);font-size:12px;cursor:pointer;font-family:inherit;margin-bottom:10px;font-weight:500;transition:all .2s}
.btn-add-record:hover{border-color:#4870a8;color:#4870a8}
.hyrox-panel{background:#060600;border-color:#1e1e00;box-shadow:0 6px 28px rgba(0,0,0,0.55)}

/* ══ LOG ══════════════════════════════════════════════════════ */
.mood-btn{background:rgba(235,245,255,0.58);border:1px solid rgba(180,200,240,0.32);border-radius:20px;padding:5px 12px;cursor:pointer;font-size:12px;transition:all .2s;display:flex;align-items:center;gap:5px;font-family:inherit;color:var(--text-m)}
.mood-btn.sel{background:rgba(150,130,210,0.13);border-color:rgba(150,130,210,0.42);color:var(--text)}
.insight-card{background:linear-gradient(135deg,rgba(200,218,252,0.28),rgba(220,200,245,0.22),rgba(245,215,238,0.18));border:1px solid rgba(190,205,240,0.40);border-radius:14px;padding:12px 14px}
.insight-label{font-size:8px;color:#7060a8;letter-spacing:3px;font-weight:600;font-family:var(--mono);margin-bottom:6px}
.prerace-card{background:linear-gradient(135deg,rgba(160,190,240,0.22),rgba(185,165,240,0.18),rgba(230,200,240,0.15));border:1px solid rgba(175,190,235,0.38);border-radius:20px;padding:16px;margin-bottom:12px;box-shadow:0 4px 20px rgba(120,140,210,0.12)}
.weekly-btn{width:100%;background:rgba(150,175,230,0.12);border:1px solid rgba(150,175,230,0.28);border-radius:14px;padding:11px;color:#0842c8;font-size:12px;cursor:pointer;font-family:var(--mono);margin-bottom:12px;font-weight:600;letter-spacing:1px;transition:all .2s}
.weekly-btn:hover{background:rgba(150,175,230,0.20)}

/* ══ FORM ════════════════════════════════════════════════════ */
.ff{margin-bottom:10px}
.fl{font-size:8px;color:rgba(8,25,65,0.52);letter-spacing:3px;font-weight:600;margin-bottom:5px;display:block;text-transform:uppercase;font-family:var(--mono)}
.fi{width:100%;background:rgba(238,246,255,0.75);border:1px solid rgba(180,200,240,0.36);border-radius:11px;padding:9px 12px;color:var(--text);font-size:13px;outline:none;transition:border-color .2s;font-family:inherit}
.fi:focus{border-color:#7aaad8;box-shadow:0 0 0 3px rgba(122,170,216,0.12)}
.fi::placeholder{color:rgba(8,25,65,0.38)}
select.fi option{background:#eaf2ff;color:var(--text)}
.frow{display:grid;grid-template-columns:1fr 1fr;gap:8px}
.brow{display:flex;gap:8px;margin-top:10px}
.bpri{flex:1;background:linear-gradient(135deg,#0842c8,#0870d0,#0858c8);border:none;border-radius:12px;padding:11px;color:#fff;font-size:13px;cursor:pointer;font-family:inherit;font-weight:600;box-shadow:0 5px 20px rgba(8,100,210,0.45)}
.bsec{flex:1;background:rgba(235,245,255,0.68);border:1px solid rgba(180,200,240,0.30);border-radius:12px;padding:11px;color:var(--text-m);font-size:13px;cursor:pointer;font-family:inherit}
.badd{width:100%;background:rgba(235,245,255,0.45);border:1px dashed rgba(160,185,230,0.28);border-radius:14px;padding:11px;color:var(--text-d);font-size:12px;cursor:pointer;font-family:inherit;margin-top:8px;font-weight:500;transition:all .2s}
.badd:hover{border-color:#4870a8;color:#4870a8}
textarea.fi{resize:none;min-height:80px}

/* ══ GOALS/QUOTES ════════════════════════════════════════════ */
.gci{display:flex;align-items:flex-start;gap:10px}.gicon{font-size:20px;margin-top:2px}.ginfo{flex:1}.gname{font-size:13px;font-weight:600;color:var(--text)}.gdays{font-size:8px;font-family:var(--mono);font-weight:600;letter-spacing:1px}.gdays.urgent{color:#b03050}.gdays.ok{color:rgba(8,25,65,0.52)}.gvals{display:flex;gap:8px;margin:5px 0 7px;align-items:center;font-size:11px;color:var(--text-m);font-family:var(--mono)}.gacts{display:flex;flex-direction:column;gap:4px}.gact{background:none;border:none;cursor:pointer;font-size:14px;padding:2px 4px;border-radius:6px;color:var(--text-d)}
.qci{display:flex;gap:10px;align-items:flex-start}.qt{font-size:12px;color:var(--text);line-height:1.78;font-style:italic;font-weight:300}.qs{font-size:9px;color:var(--text-d);margin-top:4px;font-family:var(--mono)}.qdel{background:none;border:none;color:rgba(160,60,80,0.35);cursor:pointer;font-size:16px;flex-shrink:0}

/* ══ CHAT ════════════════════════════════════════════════════ */
#coach-overlay{position:fixed;inset:0;z-index:200;display:none;flex-direction:column;background:var(--bg)}
#coach-overlay.open{display:flex;animation:fadeUp .25s ease}
.chat-hdr{flex-shrink:0;padding:14px 20px;padding-top:calc(14px + env(safe-area-inset-top,0px));border-bottom:1px solid rgba(180,200,240,0.28);display:flex;align-items:center;gap:12px;background:rgba(205,228,250,0.92);backdrop-filter:blur(20px)}
.chat-name{font-size:14px;font-weight:700;color:var(--text)}.chat-status{font-size:8px;color:#4870a8;letter-spacing:3px;font-family:var(--mono);margin-top:2px}
.chat-close{margin-left:auto;background:rgba(150,175,230,0.12);border:none;color:var(--text-m);font-size:20px;cursor:pointer;padding:5px 10px;border-radius:10px}
.chat-msgs{flex:1;overflow-y:auto;padding:14px;display:flex;flex-direction:column;gap:10px;-webkit-overflow-scrolling:touch}
.mrow{display:flex}.mrow.user{justify-content:flex-end}.mrow.ai{justify-content:flex-start;align-items:flex-end;gap:8px}
.mavatar{width:28px;height:28px;border-radius:50%;background:var(--grad);display:flex;align-items:center;justify-content:center;font-size:13px;flex-shrink:0;box-shadow:0 2px 8px rgba(120,140,210,0.3)}
.mbubble{max-width:80%;padding:10px 14px;font-size:13px;line-height:1.72;white-space:pre-wrap;font-weight:400}
.mbubble.user{background:rgba(80,160,240,0.14);border:1px solid rgba(80,160,240,0.28);border-radius:18px 18px 4px 18px;color:#081428}
.mbubble.ai{background:rgba(242,248,255,0.90);border:1px solid rgba(180,215,248,0.55);border-radius:18px 18px 18px 4px;color:#081428;box-shadow:0 2px 10px rgba(100,130,200,0.09)}
.tdots{display:flex;gap:4px;padding:3px}
.tdot{width:6px;height:6px;border-radius:50%;background:#7aaad8;animation:typing 1.2s infinite}
.tdot:nth-child(2){animation-delay:.2s}.tdot:nth-child(3){animation-delay:.4s}
.chat-bar{flex-shrink:0;padding:10px 14px;padding-bottom:calc(10px + var(--safe));border-top:1px solid rgba(180,200,240,0.28);display:flex;gap:8px;background:rgba(205,228,250,0.92);backdrop-filter:blur(20px)}
.chat-in{flex:1;background:rgba(235,245,255,0.78);border:1px solid rgba(180,200,240,0.30);border-radius:16px;padding:10px 14px;color:var(--text);font-size:14px;outline:none;font-family:inherit}
.chat-in:focus{border-color:#7aaad8}.chat-in::placeholder{color:var(--text-d)}
.chat-send{background:linear-gradient(135deg,#0842c8,#0870d0);border:none;border-radius:16px;padding:10px 16px;color:#fff;font-size:15px;cursor:pointer;box-shadow:0 4px 14px rgba(8,100,210,0.45)}
.chat-send:disabled{opacity:.35}

/* ══ FAB ══════════════════════════════════════════════════════ */
#fab{position:fixed;bottom:calc(82px + var(--safe));right:18px;width:50px;height:50px;border-radius:50%;background:var(--grad);border:none;cursor:pointer;display:flex;align-items:center;justify-content:center;font-size:20px;z-index:99;box-shadow:0 6px 24px rgba(8,100,210,0.52),0 2px 8px rgba(8,120,220,0.30),0 0 0 1.5px rgba(255,255,255,0.55);animation:gpulse 3s infinite}

/* ══ MISC ════════════════════════════════════════════════════ */
.csoon{display:flex;flex-direction:column;align-items:center;justify-content:center;min-height:60vh;gap:12px;text-align:center}
.csoon-icon{font-size:48px}.csoon-title{font-size:22px;font-weight:700;background:var(--grad);-webkit-background-clip:text;-webkit-text-fill-color:transparent}.csoon-sub{font-size:13px;color:var(--text-m);line-height:1.7;font-weight:400}
@keyframes fadeUp{from{opacity:0;transform:translateY(12px)}to{opacity:1;transform:translateY(0)}}
@keyframes gpulse{0%,100%{opacity:.82}50%{opacity:1}}
@keyframes typing{0%,80%,100%{opacity:.25}40%{opacity:1}}

/* ── Profile Page ── */
.prof-grid{display:grid;grid-template-columns:1fr 1fr;gap:6px}
.prof-item{background:rgba(140,180,230,0.08);border-radius:10px;padding:7px 10px}
.prof-label{font-size:8px;color:var(--text-d);font-family:var(--mono);letter-spacing:2px;margin-bottom:3px}
.prof-val{font-size:11px;color:var(--text);font-weight:500;font-family:var(--mono)}

/* ── API Key Setup Modal ── */
#apikey-modal{position:fixed;inset:0;z-index:999;display:flex;align-items:center;justify-content:center;background:rgba(20,28,48,0.72);backdrop-filter:blur(16px);padding:24px}
#apikey-modal.hidden{display:none}
.apikey-box{background:rgba(242,248,255,0.96);border:1px solid rgba(200,218,245,0.8);border-radius:24px;padding:28px 22px;max-width:360px;width:100%;box-shadow:0 20px 60px rgba(80,100,180,0.25)}
.apikey-title{font-size:18px;font-weight:700;color:#1a2438;margin-bottom:6px}
.apikey-sub{font-size:12px;color:rgba(26,36,56,0.55);line-height:1.7;margin-bottom:20px}
.apikey-input{width:100%;background:rgba(235,245,255,0.8);border:1px solid rgba(160,185,230,0.4);border-radius:12px;padding:11px 14px;color:#1a2438;font-size:13px;font-family:'DM Mono',monospace;outline:none;margin-bottom:8px;letter-spacing:0.5px}
.apikey-input:focus{border-color:#7aaad8;box-shadow:0 0 0 3px rgba(122,170,216,0.14)}
.apikey-hint{font-size:10px;color:rgba(26,36,56,0.38);margin-bottom:18px;font-family:'DM Mono',monospace;line-height:1.6}
.apikey-btn{width:100%;background:linear-gradient(135deg,#0842c8,#0870d0,#0858c8);border:none;border-radius:12px;padding:13px;color:#fff;font-size:14px;font-weight:600;cursor:pointer;font-family:'Outfit',sans-serif;box-shadow:0 5px 20px rgba(8,100,210,0.45)}
.apikey-btn:disabled{opacity:.4;cursor:default}
.apikey-change{margin-top:12px;text-align:center;font-size:11px;color:rgba(26,36,56,0.38);cursor:pointer;font-family:'DM Mono',monospace}
.apikey-change:hover{color:#4870a8}
</style>
</head>
<body>
<div class="mesh"></div>
<div class="stars" id="stars"></div>
<div id="app">
  <div id="hdr">
    <div><div class="logo-sub">Personal Training AI</div><div class="logo-main">Coach Claude</div></div>
    <div style="text-align:right">
      <div style="font-size:8px;color:var(--text-d);font-family:var(--mono);letter-spacing:2px">CATHY · 2026</div>
      <div style="display:flex;align-items:center;gap:8px;justify-content:flex-end;margin-top:4px">
        <div class="sdot"></div><span class="stxt">ACTIVE</span>
        <button onclick="showApiKeySettings()" style="background:rgba(140,180,230,0.15);border:1px solid rgba(140,180,230,0.25);border-radius:8px;padding:2px 8px;font-size:9px;color:#0842c8;cursor:pointer;font-family:var(--mono)">KEY</button>
      </div>
    </div>
  </div>

  <div id="content">
    <div class="page active" id="page-home">
      <div id="prerace-module" style="display:none"></div>
      <div class="card iglow" id="coach-card">
        <div style="display:flex;gap:12px;align-items:flex-start">
          <div class="cavatar">☁️</div>
          <div style="flex:1">
            <div class="slabel si" style="margin-bottom:6px">Coach Cloudy Says</div>
            <div id="coach-msg" style="font-size:13px;color:var(--text);line-height:1.8;font-weight:400"><span style="color:var(--text-d)">Loading...</span></div>
          </div>
        </div>
      </div>

      <div class="slabel si">Your Journey</div>
      <div class="journey-grid">

        <!-- NOW panel -->
        <div class="jpanel">
          <div class="jlabel now">Now</div>
          <div class="ring-wrap">
            <svg width="118" height="118" viewBox="0 0 118 118">
              <defs>
                <!-- Pearl iridescent gradients — Now -->
                <linearGradient id="np1" x1="0%" y1="0%" x2="100%" y2="100%">
                  <stop offset="0%" stop-color="#6898c8"/>
                  <stop offset="35%" stop-color="#9898d8"/>
                  <stop offset="65%" stop-color="#c8a8d8"/>
                  <stop offset="100%" stop-color="#7888c8"/>
                </linearGradient>
                <linearGradient id="np2" x1="0%" y1="0%" x2="100%" y2="100%">
                  <stop offset="0%" stop-color="#8898d0"/>
                  <stop offset="40%" stop-color="#b8a8e0"/>
                  <stop offset="75%" stop-color="#d8b8e8"/>
                  <stop offset="100%" stop-color="#9888c8"/>
                </linearGradient>
                <linearGradient id="np3" x1="0%" y1="0%" x2="100%" y2="100%">
                  <stop offset="0%" stop-color="#7898c0"/>
                  <stop offset="45%" stop-color="#a8a0d8"/>
                  <stop offset="80%" stop-color="#c8a0c8"/>
                  <stop offset="100%" stop-color="#8888b8"/>
                </linearGradient>
                <!-- Shimmer highlight gradient -->
                <linearGradient id="nsh1" x1="0%" y1="0%" x2="100%" y2="0%">
                  <stop offset="0%" stop-color="white" stop-opacity="0"/>
                  <stop offset="50%" stop-color="white" stop-opacity="0.7"/>
                  <stop offset="100%" stop-color="white" stop-opacity="0"/>
                </linearGradient>
              </defs>
              <!-- Track rings -->
              <circle cx="59" cy="59" r="50" fill="none" stroke="rgba(120,160,220,0.13)" stroke-width="9"/>
              <circle cx="59" cy="59" r="38" fill="none" stroke="rgba(150,140,215,0.11)" stroke-width="8"/>
              <circle cx="59" cy="59" r="27" fill="none" stroke="rgba(130,150,215,0.10)" stroke-width="7"/>
              <!-- Pearl arcs -->
              <circle cx="59" cy="59" r="50" fill="none" stroke="url(#np1)" stroke-width="9" stroke-dasharray="314" stroke-dashoffset="195" stroke-linecap="round" transform="rotate(-90 59 59)" opacity="0.78"/>
              <circle cx="59" cy="59" r="38" fill="none" stroke="url(#np2)" stroke-width="8" stroke-dasharray="239" stroke-dashoffset="155" stroke-linecap="round" transform="rotate(-90 59 59)" opacity="0.72"/>
              <circle cx="59" cy="59" r="27" fill="none" stroke="url(#np3)" stroke-width="7" stroke-dasharray="170" stroke-dashoffset="72" stroke-linecap="round" transform="rotate(-90 59 59)" opacity="0.68"/>
              <!-- Chrome highlight streaks -->
              <circle cx="59" cy="59" r="50" fill="none" stroke="rgba(255,255,255,0.62)" stroke-width="2.5" stroke-dasharray="20 294" stroke-dashoffset="195" stroke-linecap="round" transform="rotate(-90 59 59)"/>
              <circle cx="59" cy="59" r="38" fill="none" stroke="rgba(255,255,255,0.55)" stroke-width="2" stroke-dasharray="15 224" stroke-dashoffset="155" stroke-linecap="round" transform="rotate(-90 59 59)"/>
              <circle cx="59" cy="59" r="27" fill="none" stroke="rgba(255,255,255,0.50)" stroke-width="1.5" stroke-dasharray="10 160" stroke-dashoffset="72" stroke-linecap="round" transform="rotate(-90 59 59)"/>
              <!-- Center -->
              <text x="59" y="62" text-anchor="middle" font-size="22" font-weight="700" fill="#1a2438" font-family="DM Mono,monospace" id="now-center">52.8</text>
              <text x="59" y="76" text-anchor="middle" font-size="9" fill="rgba(26,36,56,0.35)" font-family="DM Mono,monospace">kg</text>
            </svg>
          </div>
          <div class="jbadges"><span class="jbadge bi">D-type</span><span class="jbadge bi">80 pts</span></div>
          <div class="jstats" id="stats-now"></div>
          <div class="jdate">2026.04.04</div>
        </div>

        <div class="arrow-mid"><div class="arrow-c">→</div></div>

        <!-- GOAL panel: full pearl chrome iridescent -->
        <div class="jpanel goal" id="goal-panel">
          <div class="jlabel goal">Goal</div>
          <div class="ring-wrap">
            <svg class="goal-svg" width="118" height="118" viewBox="0 0 118 118">
              <defs>
                <!-- 4-stop iridescent: ice→violet→rose→silver -->
                <linearGradient id="gp1" x1="0%" y1="0%" x2="100%" y2="100%">
                  <stop offset="0%"   stop-color="#7ab0e8"/>
                  <stop offset="28%"  stop-color="#a898e8"/>
                  <stop offset="55%"  stop-color="#d8a8d8"/>
                  <stop offset="78%"  stop-color="#e8c8e0"/>
                  <stop offset="100%" stop-color="#8898d8"/>
                </linearGradient>
                <linearGradient id="gp2" x1="0%" y1="0%" x2="100%" y2="100%">
                  <stop offset="0%"   stop-color="#9890e0"/>
                  <stop offset="30%"  stop-color="#c8a8e8"/>
                  <stop offset="58%"  stop-color="#e0b8e8"/>
                  <stop offset="80%"  stop-color="#f0d0f0"/>
                  <stop offset="100%" stop-color="#a898d8"/>
                </linearGradient>
                <linearGradient id="gp3" x1="0%" y1="0%" x2="100%" y2="100%">
                  <stop offset="0%"   stop-color="#6898d0"/>
                  <stop offset="32%"  stop-color="#98a0e0"/>
                  <stop offset="60%"  stop-color="#c8a8e0"/>
                  <stop offset="82%"  stop-color="#d8c0e8"/>
                  <stop offset="100%" stop-color="#8888c8"/>
                </linearGradient>
                <!-- Rotating shimmer gradient -->
                <linearGradient id="spin1" x1="0%" y1="0%" x2="100%" y2="0%">
                  <stop offset="0%"   stop-color="white" stop-opacity="0"/>
                  <stop offset="40%"  stop-color="white" stop-opacity="0.85"/>
                  <stop offset="55%"  stop-color="white" stop-opacity="0.95"/>
                  <stop offset="100%" stop-color="white" stop-opacity="0"/>
                </linearGradient>
                <filter id="gblur"><feGaussianBlur stdDeviation="0.7"/></filter>
              </defs>

              <!-- Track -->
              <circle cx="59" cy="59" r="50" fill="none" stroke="rgba(150,165,235,0.16)" stroke-width="11"/>
              <circle cx="59" cy="59" r="38" fill="none" stroke="rgba(170,148,235,0.14)" stroke-width="10"/>
              <circle cx="59" cy="59" r="27" fill="none" stroke="rgba(140,160,235,0.14)" stroke-width="9"/>

              <!-- Pearl arcs with blur for glow depth -->
              <circle cx="59" cy="59" r="50" fill="none" stroke="url(#gp1)" stroke-width="11" stroke-dasharray="314" stroke-dashoffset="42" stroke-linecap="round" transform="rotate(-90 59 59)" filter="url(#gblur)"/>
              <circle cx="59" cy="59" r="38" fill="none" stroke="url(#gp2)" stroke-width="10" stroke-dasharray="239" stroke-dashoffset="33" stroke-linecap="round" transform="rotate(-90 59 59)" filter="url(#gblur)"/>
              <circle cx="59" cy="59" r="27" fill="none" stroke="url(#gp3)" stroke-width="9"  stroke-dasharray="170" stroke-dashoffset="22" stroke-linecap="round" transform="rotate(-90 59 59)" filter="url(#gblur)"/>

              <!-- Crisp arc on top -->
              <circle cx="59" cy="59" r="50" fill="none" stroke="url(#gp1)" stroke-width="11" stroke-dasharray="314" stroke-dashoffset="42" stroke-linecap="round" transform="rotate(-90 59 59)" opacity="0.85"/>
              <circle cx="59" cy="59" r="38" fill="none" stroke="url(#gp2)" stroke-width="10" stroke-dasharray="239" stroke-dashoffset="33" stroke-linecap="round" transform="rotate(-90 59 59)" opacity="0.82"/>
              <circle cx="59" cy="59" r="27" fill="none" stroke="url(#gp3)" stroke-width="9"  stroke-dasharray="170" stroke-dashoffset="22" stroke-linecap="round" transform="rotate(-90 59 59)" opacity="0.78"/>

              <!-- Chrome streak highlights — thick bright white -->
              <circle cx="59" cy="59" r="50" fill="none" stroke="rgba(255,255,255,0.90)" stroke-width="4" stroke-dasharray="26 288" stroke-dashoffset="42" stroke-linecap="round" transform="rotate(-90 59 59)"/>
              <circle cx="59" cy="59" r="38" fill="none" stroke="rgba(255,255,255,0.82)" stroke-width="3.2" stroke-dasharray="19 220" stroke-dashoffset="33" stroke-linecap="round" transform="rotate(-90 59 59)"/>
              <circle cx="59" cy="59" r="27" fill="none" stroke="rgba(255,255,255,0.76)" stroke-width="2.5" stroke-dasharray="13 157" stroke-dashoffset="22" stroke-linecap="round" transform="rotate(-90 59 59)"/>

              <!-- Secondary shimmer at tail -->
              <circle cx="59" cy="59" r="50" fill="none" stroke="rgba(230,220,255,0.50)" stroke-width="1.8" stroke-dasharray="10 304" stroke-dashoffset="185" stroke-linecap="round" transform="rotate(-90 59 59)"/>
              <circle cx="59" cy="59" r="38" fill="none" stroke="rgba(220,205,255,0.42)" stroke-width="1.4" stroke-dasharray="7 232" stroke-dashoffset="142" stroke-linecap="round" transform="rotate(-90 59 59)"/>

              <!-- Star sparkles -->
              <g opacity="0.92"><line x1="59" y1="5" x2="59" y2="12" stroke="white" stroke-width="1.8" stroke-linecap="round"/><line x1="55.5" y1="8.5" x2="62.5" y2="8.5" stroke="white" stroke-width="1.8" stroke-linecap="round"/></g>
              <g opacity="0.78"><line x1="108" y1="31" x2="108" y2="37" stroke="white" stroke-width="1.4" stroke-linecap="round"/><line x1="105" y1="34" x2="111" y2="34" stroke="white" stroke-width="1.4" stroke-linecap="round"/></g>
              <g opacity="0.72"><line x1="10" y1="85" x2="10" y2="90" stroke="#d0e0ff" stroke-width="1.3" stroke-linecap="round"/><line x1="7.5" y1="87.5" x2="12.5" y2="87.5" stroke="#d0e0ff" stroke-width="1.3" stroke-linecap="round"/></g>
              <circle cx="104" cy="82" r="1.8" fill="white" opacity="0.72"/>
              <circle cx="14"  cy="35" r="1.4" fill="#c8d8f8" opacity="0.65"/>
              <circle cx="88"  cy="108" r="1.2" fill="white" opacity="0.58"/>

              <!-- Center -->
              <text x="59" y="62" text-anchor="middle" font-size="24" font-weight="700" fill="#1a2438" font-family="DM Mono,monospace" id="goal-center">48</text>
              <text x="59" y="76" text-anchor="middle" font-size="9" fill="rgba(100,120,200,0.65)" font-family="DM Mono,monospace">kg</text>
            </svg>
          </div>
          <div class="jbadges"><span class="jbadge bi">D-type ✦</span><span class="jbadge bv">Abs ✦</span><span class="jbadge by">Podium</span></div>
          <div class="jstats" id="stats-goal"></div>
          <div class="jdate">Sep · 2026</div>
        </div>
      </div>

      <div class="card">
        <div class="slabel si">Progress</div>
        <div id="prog-bars"></div>
        <div style="display:flex;justify-content:space-between;padding-top:8px;border-top:1px solid rgba(150,180,230,0.18);margin-top:4px">
          <span style="font-size:9px;color:var(--text-d);font-family:var(--mono)">Day one.</span>
          <span style="font-size:9px;font-family:var(--mono);font-weight:600;background:var(--grad);-webkit-background-clip:text;-webkit-text-fill-color:transparent" id="days-left"></span>
        </div>
      </div>

      <div class="card"><div class="slabel sb">Race Countdown</div><div id="race-countdown"></div></div>

      <button class="weekly-btn" id="weekly-report-btn" onclick="generateWeeklyReport()">📋 Weekly Report</button>
      <div id="weekly-report-result"></div>

      <div class="card spark"><div class="slabel sk">Daily Spark</div><div class="spark-q" id="spark-q"></div><div class="spark-src" id="spark-src"></div></div>

      <div class="card orbit">
        <div class="orbit-hdr" onclick="toggleOrbit()">
          <div class="slabel sv" style="margin-bottom:0;flex:1">🔮 Weekly Orbit</div>
          <span class="orbit-chev" id="orbit-chev">▾</span>
        </div>
        <div class="orbit-body" id="orbit-body"><div style="height:10px"></div><div id="orbit-content"></div></div>
      </div>
    </div>

    <div class="page" id="page-record"></div>
    <div class="page" id="page-goals">
      <div class="slabel si" style="margin-bottom:14px">Body Goals</div><div id="body-goals-list"></div>
      <div class="slabel sv" style="margin:20px 0 12px">Race Goals</div><div id="race-goals-list"></div>
      <button class="badd" onclick="showGoalForm()">+ Add Goal</button>
      <div id="goal-form-wrap" style="margin-top:10px"></div>
      <div class="slabel sk" style="margin:28px 0 12px">Daily Spark Library</div>
      <div id="quotes-list"></div>
      <button class="badd" onclick="showQuoteForm()">+ Add Quote</button>
      <div id="quote-form-wrap" style="margin-top:10px"></div>
    </div>
    <div class="page" id="page-race"></div>
    <div class="page" id="page-race-detail">
      <div style="display:flex;align-items:center;gap:12px;margin-bottom:18px">
        <button class="back-btn" onclick="backToRace()">← Back</button>
        <div style="font-size:20px;font-weight:700;color:var(--text);flex:1" id="detail-title"></div>
        <div id="detail-icon" style="font-size:26px"></div>
      </div>
      <div id="race-detail-content"></div>
    </div>
    <div class="page" id="page-profile"></div>
    <div class="page" id="page-trend"><div class="csoon"><div class="csoon-icon">📈</div><div class="csoon-title">Trends</div><div class="csoon-sub">Body fat · Muscle · Weekly volume</div></div></div>
  </div>

  <div id="nav">
    <button class="nb active" onclick="switchTab('home',this)"><span class="ni">🏠</span><span class="nl">Home</span><div class="nbar"></div></button>
    <button class="nb" onclick="switchTab('record',this)"><span class="ni">📊</span><span class="nl">Log</span><div class="nbar"></div></button>
    <button class="nb" onclick="switchTab('goals',this)"><span class="ni">🎯</span><span class="nl">Goals</span><div class="nbar"></div></button>
    <button class="nb" onclick="switchTab('race',this)"><span class="ni">🏆</span><span class="nl">Race</span><div class="nbar"></div></button>
    <button class="nb" onclick="switchTab('profile',this)"><span class="ni">✦</span><span class="nl">Profile</span><div class="nbar"></div></button>
  </div>
</div>

<button id="fab" onclick="openCoach()">☁️</button>
<div id="coach-overlay">
  <div class="chat-hdr">
    <div class="cavatar">☁️</div>
    <div><div class="chat-name">Coach Cloudy</div><div class="chat-status">ON DUTY · 24/7</div></div>
    <button class="chat-close" onclick="closeCoach()">✕</button>
  </div>
  <div class="chat-msgs" id="chat-msgs"></div>
  <div class="chat-bar">
    <input class="chat-in" id="chat-in" placeholder="Ask your coach..." onkeydown="if(event.key==='Enter')sendMsg()">
    <button class="chat-send" id="chat-send" onclick="sendMsg()">↑</button>
  </div>
</div>

<!-- API Key Modal -->
<div id="apikey-modal" class="hidden">
  <div class="apikey-box">
    <div style="font-size:32px;margin-bottom:12px">☁️</div>
    <div class="apikey-title">歡迎，Cathy</div>
    <div class="apikey-sub">Coach Cloudy 需要 API Key 才能運作。<br>Key 只存在你的手機裡，不會上傳到任何地方。</div>
    <input class="apikey-input" id="apikey-input" type="password" placeholder="sk-ant-api03-..." autocomplete="off" spellcheck="false">
    <div class="apikey-hint">
      取得方式：console.anthropic.com → API Keys → Create Key<br>
      費用：每月約 $0.5–2 美金（台幣 15–60 元）
    </div>
    <button class="apikey-btn" id="apikey-btn" onclick="saveApiKey()">開始訓練 →</button>
    <div class="apikey-change" onclick="skipApiKey()">稍後再說</div>
  </div>
</div>

<script>
// ── Sparkle stars ──────────────────────────────────────────────
(function(){
  const c=document.getElementById('stars');
  const S=[
    [2,0.85,'4s','0s'],[1.5,0.68,'5.5s','0.8s'],[2.5,0.92,'3.2s','1.5s'],
    [1.5,0.72,'6.5s','0.3s'],[2,0.78,'4.8s','2.1s'],[1.5,0.62,'7.2s','1.1s'],
    [3,0.88,'3.5s','0.6s'],[1.5,0.70,'5.8s','2.4s'],[2,0.82,'4.2s','0.9s'],
    [1.5,0.60,'6.8s','1.8s'],[2.5,0.90,'3.8s','0.4s'],[1.5,0.66,'5.2s','2.8s'],
    [2,0.75,'4.6s','1.3s'],[1.8,0.80,'3.9s','3.0s'],[2.2,0.85,'5.0s','0.7s'],
  ];
  S.forEach(([w,op,d,dl],i)=>{
    const el=document.createElement('div');
    el.className='star';
    const x=5+Math.random()*90,y=5+Math.random()*90;
    el.style.cssText=`left:${x}%;top:${y}%;width:${w}px;height:${w}px;--op:${op};--d:${d};--dl:${dl};background:white;box-shadow:0 0 ${w*2.5}px ${w}px rgba(200,218,255,0.7)`;
    c.appendChild(el);
  });
})();

const LS={get(k,d){try{const v=localStorage.getItem(k);return v?JSON.parse(v):d}catch{return d}},set(k,v){try{localStorage.setItem(k,JSON.stringify(v))}catch{}}};
const DEF_GOALS={body:[{id:1,label:'Weight',current:52.8,target:48.0,unit:'kg',deadline:'2026-09-01',icon:'⚖️'},{id:2,label:'Body Fat',current:20.0,target:18.0,unit:'%',deadline:'2026-09-01',icon:'🔥'},{id:3,label:'Muscle',current:23.2,target:24.5,unit:'kg',deadline:'2026-09-01',icon:'💪'}],race:[{id:4,label:'Half Marathon',current:null,target:'Sub 1:40',unit:'',deadline:'2026-12-31',icon:'🏃'},{id:5,label:'Hyrox',current:null,target:'Finish strong',unit:'',deadline:'2026-12-31',icon:'🔱'},{id:6,label:'TIS 桃園二鐵',current:null,target:'分組獎牌',unit:'',deadline:'2025-10-04',icon:'🏅'}]};
const DEF_QUOTES=[{id:1,text:'你還差得遠——但你做得到。現在去練。',source:'Coach Cloudy'},{id:2,text:'痛苦是暫時的，放棄是永遠的。',source:'Lance Armstrong'},{id:3,text:'冠軍不是在賽場上造就的，而是在那些無人注視的訓練中鍛造的。',source:'Muhammad Ali'}];
const FP=[{body:'本週身體狀態極佳，肌肉吸收效率高，是加大訓練量的好時機。',sport:'心肺運動特別加分，長跑或有氧能帶來超預期成效。',race:'比賽運勢穩定，專注執行計畫，結果不讓你失望。',beauty:'氣色明亮，訓練後的光澤感特別明顯。',action:'今天去完成一件你一直在拖延的訓練。'},{body:'能量儲備充足，適合挑戰個人最佳紀錄。',sport:'爆發力運動有意外驚喜，可以嘗試突破重量極限。',race:'本週有貴人相助，和隊友訓練效果加倍。',beauty:'運動後的自信感特別強烈，這是你最美的狀態。',action:'找一個訓練夥伴，今天一起突破極限。'},{body:'身體正在深層修復，恢復品質決定下次爆發。',sport:'技術細節比強度更重要，專注姿勢和呼吸。',race:'靜水流深，低調積累實力，比賽時自然爆發。',beauty:'睡眠充足才是最好的保養，今晚早點休息。',action:'今天做一次完整拉伸和恢復，投資未來的你。'}];
const RACE_DATA={hyrox:{id:'hyrox',name:'Hyrox',icon:'🔱',isHyrox:true,desc:'8 functional stations · 8×1km runs',target:'首次完賽，建立基準時間',training:[{icon:'🚣',name:'SkiErg · Row Machine',tag:'hyrox-tag',tl:'Hyrox'},{icon:'🛷',name:'Sled Push · Pull',tag:'hyrox-tag',tl:'Hyrox'},{icon:'🏋️',name:'Burpee Broad Jump',tag:'hyrox-tag',tl:'Hyrox'},{icon:'🪣',name:'Wall Balls',tag:'hyrox-tag',tl:'Hyrox'},{icon:'🧳',name:'Farmers Carry',tag:'hyrox-tag',tl:'Hyrox'},{icon:'💪',name:'Sandbag Lunges',tag:'hyrox-tag',tl:'Hyrox'},{icon:'🏃',name:'1km Run × 8',tag:'running-tag',tl:'Cardio'}]},running:{id:'running',name:'Running',icon:'🏃',isHyrox:false,desc:'Half marathon · Trail · Speed',target:'半馬破100分鐘 · 上坡不喘',training:[{icon:'🏃',name:'Zone 2 Long Run 60min+',tag:'running-tag',tl:'Base'},{icon:'⚡',name:'Tempo Run 20–30min',tag:'running-tag',tl:'Speed'},{icon:'🔁',name:'Interval 400m×8',tag:'running-tag',tl:'Interval'},{icon:'⛰️',name:'Hill Repeats',tag:'running-tag',tl:'Climb'},{icon:'💨',name:'Strides',tag:'running-tag',tl:'Speed'}]},duathlon:{id:'duathlon',name:'TIS 桃園二鐵',icon:'🏅',isHyrox:false,desc:'跑步 5.4km → 騎車 30.7km → 跑步 5.4km',target:'2025.10.04 · 分組獎牌',training:[{icon:'🚴',name:'FTP 功率騎訓練 30.7km',tag:'duathlon-tag',tl:'Bike'},{icon:'🔄',name:'Brick 磚塊訓練（騎→跑）',tag:'duathlon-tag',tl:'Combo'},{icon:'🏃',name:'跑步訓練 5.4km×2',tag:'duathlon-tag',tl:'Run'},{icon:'⚡',name:'換項練習 T1',tag:'duathlon-tag',tl:'Transition'},{icon:'📍',name:'竹圍漁港 · 大園區',tag:'duathlon-tag',tl:'Venue'}]}};
const EMOJI_MOODS=[{id:'pumped',label:'拼了',emoji:'😤',color:'#6888c8'},{id:'good',label:'狀態好',emoji:'🔥',color:'#5878c0'},{id:'steady',label:'平穩',emoji:'😌',color:'#9890d8'},{id:'tired',label:'疲憊',emoji:'😴',color:'#a898d0'},{id:'struggle',label:'掙扎中',emoji:'😟',color:'#8870b8'}];
const ACT_TYPES=['Cross','CrossFit','Running','Hyrox','Duathlon','Yoga/Stretch','Other'];
let goals=LS.get('cc_goals',DEF_GOALS),quotes=LS.get('cc_quotes',DEF_QUOTES),chatHistory=[],coachLoading=false,selectedMood='steady',uploadedImages=[];

function daysTo(d){if(!d)return null;return Math.ceil((new Date(d)-new Date())/86400000)}
function buildSys(){const bg=goals.body.map(g=>`${g.label}: ${g.current}${g.unit}→${g.target}${g.unit}`).join('; ');const rg=goals.race.map(g=>`${g.label}: target ${g.target}`).join('; ');const qs=quotes.map(q=>`「${q.text}」——${q.source}`).join('；');return `你是Cathy的私人綜合教練Coach Cloudy，同時扮演三個嚴格角色：\n【重量訓練教練】體能評估、訓練菜單、動作邏輯、訓練量控管\n【跑步教練】半馬破100分鐘、越野賽出色表現、週期化訓練、比賽策略\n【營養師】保肌減脂、可長期執行、顧氣色不顯老\n體態目標：${bg}\n比賽目標：${rg}\n額外目標：Hyrox個人+混雙拿得出手、體操pull up、倒立
2025/10/04 TIS桃園二鐵：跑5.4km→騎30.7km→跑5.4km，目標分組獎牌，地點竹圍漁港\n現況：D-type體型，體脂20%，骨骼肌23.2kg，InBody 80分\n她的金句：${qs}\n回應原則：
- 帥氣霸道，說話簡短有力，每句都有份量
- 直接指出問題，不廢話，但讓人感覺被懂、不是被罵
- 嚴格中偶爾藏一句暖的，不濫情但有溫度
- 像那種你知道他是真心為你好的教練，不是冷漠的機器
- 繁體中文，120字以內，不用條列，說話像人不像報告`}
async function callAI(messages,system){
  const key=getApiKey();
  if(!key){showApiKeySettings();throw new Error('No API key')}
  const r=await fetch('https://api.anthropic.com/v1/messages',{
    method:'POST',
    headers:{'Content-Type':'application/json','x-api-key':key,'anthropic-version':'2023-06-01','anthropic-dangerous-direct-browser-access':'true'},
    body:JSON.stringify({model:'claude-sonnet-4-20250514',max_tokens:1000,system,messages})
  });
  const d=await r.json();
  if(d.error){throw new Error(d.error.message)}
  return d.content?.find(b=>b.type==='text')?.text||''
}

function renderDashboard(){
  const bw=goals.body.find(g=>g.label==='Weight'),bf=goals.body.find(g=>g.label==='Body Fat'),mu=goals.body.find(g=>g.label==='Muscle');
  if(bw){document.getElementById('now-center').textContent=bw.current;document.getElementById('goal-center').textContent=bw.target}
  document.getElementById('stats-now').innerHTML=[{dot:'#7898c8',label:'Body Fat',val:`${bf?.current||'—'}%`,cls:'vi'},{dot:'#9890d8',label:'Muscle',val:`${mu?.current||'—'}kg`,cls:'vv'},{dot:'#a8a0d8',label:'VO₂Max',val:'—',cls:'vb'},{dot:'rgba(150,170,230,0.6)',label:'Visceral Fat',val:'Lv.4',cls:'vi'}].map(s=>`<div class="jstat"><div class="jsdot" style="background:${s.dot}"></div><span class="jslabel">${s.label}</span><span class="jsval ${s.cls}">${s.val}</span></div>`).join('');
  document.getElementById('stats-goal').innerHTML=[{dot:'#7898c8',label:'Body Fat',val:`${bf?.target||'18'}%`,cls:'vi'},{dot:'#b0a0e0',label:'Muscle',val:`${mu?.target||'24.5'}kg`,cls:'vv'},{dot:'#c8b0e0',label:'VO₂Max',val:'↑ Target',cls:'vk'},{dot:'#d8b8e8',label:'Abs',val:'Visible ✦',cls:'vk'}].map(s=>`<div class="jstat"><div class="jsdot" style="background:${s.dot}"></div><span class="jslabel">${s.label}</span><span class="jsval ${s.cls}">${s.val}</span></div>`).join('');
  const cols=['linear-gradient(135deg,#7898c8,#a8a0d8)','linear-gradient(135deg,#a090d8,#c8a8e0)','linear-gradient(135deg,#7898c0,#b8a8e0)'];
  document.getElementById('prog-bars').innerHTML=goals.body.map((g,i)=>`<div style="margin-bottom:8px"><div style="display:flex;justify-content:space-between;margin-bottom:4px"><span style="font-size:9px;color:var(--text-m);font-family:var(--mono)">${g.icon} ${g.label}</span><span style="font-size:9px;color:var(--text-d);font-family:var(--mono)">${g.current}${g.unit} → ${g.target}${g.unit}</span></div><div class="ptrack"><div class="pfill" style="width:0%;background:${cols[i]}" data-t="5" data-d="${i*200}"></div></div></div>`).join('');
  setTimeout(()=>document.querySelectorAll('.pfill').forEach(el=>setTimeout(()=>el.style.width=el.dataset.t+'%',+el.dataset.d)),300);
  const md=goals.race.find(g=>g.deadline)?.deadline||'2026-09-01';
  document.getElementById('days-left').textContent=`${daysTo(md)} DAYS TO GO`;
  document.getElementById('race-countdown').innerHTML=goals.race.map(g=>{const d=daysTo(g.deadline);const rk=g.label.toLowerCase().includes('hyrox')?'hyrox':g.label.toLowerCase().includes('run')?'running':'duathlon';return`<div class="race-item" onclick="openRaceDetail('${rk}')"><span class="ri-icon">${g.icon}</span><div class="ri-info"><div class="ri-name">${g.label}</div><div class="ri-target">${g.target}</div></div>${d!==null?`<div style="text-align:right"><div class="ri-num">${d}</div><div class="ri-unit">days</div></div>`:''}<span class="ri-chev">›</span></div>`}).join('');
  const q=quotes[Math.floor(Math.random()*quotes.length)]||DEF_QUOTES[0];
  document.getElementById('spark-q').textContent=`「${q.text}」`;document.getElementById('spark-src').textContent=q.source?`— ${q.source}`:'';
  const f=FP[Math.floor(Math.random()*FP.length)];
  document.getElementById('orbit-content').innerHTML=[['✨ 體態 · 美貌',f.beauty],['⚡ 運動表現',f.sport],['🏆 比賽運勢',f.race],['💎 身體狀態',f.body]].map(([l,t])=>`<div class="orbit-sec"><div class="orbit-sec-label">${l}</div><div class="orbit-sec-text">${t}</div></div>`).join('')+`<div class="orbit-action"><div class="orbit-act-label">✦ 本週行動</div><div class="orbit-act-text">${f.action}</div></div>`;
  fetchCoachMsg();checkPreRace();
}

async function fetchCoachMsg(){const el=document.getElementById('coach-msg');el.innerHTML='<span style="color:var(--text-d)">Loading...</span>';try{const msg=await callAI([{role:'user',content:'今日提醒'}],buildSys()+'\n給一句今日激勵提醒，直接有力，50字以內。');el.textContent=msg||'距離目標還差4.8公斤。今天的訓練，決定9月的你。';}catch{el.textContent='目標不等人。今天完成訓練，明天的你會感謝現在的你。'}}
function checkPreRace(){const el=document.getElementById('prerace-module');if(!el)return;let closest=null,minDays=999;goals.race.forEach(g=>{const d=daysTo(g.deadline);if(d!==null&&d<=7&&d>=0&&d<minDays){minDays=d;closest=g}});if(!closest){el.style.display='none';return}el.style.display='block';const rk=closest.label.toLowerCase().includes('hyrox')?'hyrox':closest.label.toLowerCase().includes('run')?'running':'duathlon';el.innerHTML=`<div class="prerace-card"><div style="display:flex;align-items:center;gap:8px;margin-bottom:10px"><span style="font-size:20px">${closest.icon}</span><div><div style="font-size:8px;color:#7060a8;letter-spacing:3px;font-family:var(--mono);font-weight:600">RACE WEEK</div><div style="font-size:14px;font-weight:700;color:var(--text)">${closest.label} · ${minDays===0?'今天！':`${minDays} days`}</div></div></div><div id="prerace-msg" style="font-size:13px;color:var(--text-m);line-height:1.8;font-weight:400"><span style="color:var(--text-d)">載入中...</span></div><button onclick="loadPreRaceMsg('${rk}',${minDays})" class="bpri" style="font-size:12px;padding:8px 14px;margin-top:10px;width:auto">☁️ 賽前心理建設</button></div>`}
async function loadPreRaceMsg(rk,dl){const el=document.getElementById('prerace-msg');if(!el)return;el.innerHTML='<span style="color:var(--text-d)">思考中...</span>';const r=RACE_DATA[rk];const phase=dl===0?'比賽當天':dl===1?'比賽前一天':dl<=3?'賽前三天':'賽前一週';try{const msg=await callAI([{role:'user',content:`距離${r.name}還有${dl}天，現在是${phase}，給我賽前心理建設。`}],buildSys());el.textContent=msg||'你準備好了。去做就對了。';}catch{el.textContent='你已經準備好了。相信訓練。'}}
async function generateWeeklyReport(){const btn=document.getElementById('weekly-report-btn');if(btn){btn.disabled=true;btn.textContent='Generating...'}const logs=LS.get('cc_logs',[]);const owa=new Date();owa.setDate(owa.getDate()-7);const wl=logs.filter(l=>new Date(l.date)>=owa);const sum=wl.length?wl.map(l=>`${l.date} ${l.actType} [${l.mood}]: ${l.note||'(圖片)'}`).join('\n'):'本週無記錄';try{const msg=await callAI([{role:'user',content:`本週訓練（${wl.length}筆）：\n${sum}\n\n請生成本週週報。`}],buildSys()+'\n作為三合一教練，綜合本週訓練給週報：說好的、說問題、給下週重點建議。200字以內。');document.getElementById('weekly-report-result').innerHTML=`<div class="card pglow" style="margin-bottom:12px"><div style="display:flex;align-items:center;gap:10px;margin-bottom:10px"><div class="cavatar" style="width:30px;height:30px;font-size:14px">☁️</div><div><div style="font-size:8px;color:#7060a8;letter-spacing:3px;font-family:var(--mono);font-weight:600">WEEKLY REPORT</div><div style="font-size:8px;color:var(--text-d);font-family:var(--mono)">${new Date().toLocaleDateString('zh-TW')}</div></div></div><div style="font-size:13px;color:var(--text);line-height:1.85;white-space:pre-wrap;font-weight:400">${msg}</div><div style="margin-top:10px;font-size:8px;color:var(--text-d);font-family:var(--mono)">本週訓練 ${wl.length} 次</div></div>`;}catch{document.getElementById('weekly-report-result').innerHTML=''}if(btn){btn.disabled=false;btn.textContent='📋 Weekly Report'}}
function toggleOrbit(){
  const body=document.getElementById('orbit-body');
  const wasOpen=body.classList.contains('open');
  body.classList.toggle('open');
  document.getElementById('orbit-chev').classList.toggle('open');
  // Generate AI orbit on first open
  if(!wasOpen && !body.dataset.loaded){
    body.dataset.loaded='1';
    generateOrbit();
  }
}
function renderLogPage(){const logs=LS.get('cc_logs',[]);document.getElementById('page-record').innerHTML=`<div class="slabel si" style="margin-bottom:14px">Log</div><button class="bpri" style="width:100%;margin-bottom:16px" onclick="showLogForm()">+ 記錄今天的訓練</button><div id="log-form-wrap"></div><div id="log-history">${logs.length===0?`<div style="text-align:center;padding:40px 0;color:var(--text-d);font-size:13px;font-family:var(--mono)">No entries yet.</div>`:logs.map(renderLogCard).join('')}</div>${logs.length>=3?`<div style="margin-top:8px"><button class="weekly-btn" onclick="analyzePattern()">🔍 Analyze My Pattern</button><div id="pattern-result"></div></div>`:''}`}
function renderLogCard(log){const mood=EMOJI_MOODS.find(m=>m.id===log.mood)||EMOJI_MOODS[2];return`<div class="card" style="margin-bottom:10px;border-left:2px solid ${mood.color}"><div style="display:flex;align-items:center;gap:8px;margin-bottom:8px"><span style="font-size:18px">${mood.emoji}</span><div style="flex:1"><div style="font-size:12px;font-weight:600;color:var(--text)">${log.actType||'Training'}</div><div style="font-size:8px;color:var(--text-d);font-family:var(--mono);letter-spacing:1px">${log.date}</div></div><button onclick="deleteLog(${log.id})" style="background:none;border:none;color:rgba(150,60,80,0.35);font-size:15px;cursor:pointer">×</button></div>${log.note?`<div style="font-size:12px;color:var(--text-m);line-height:1.7;margin-bottom:10px;font-weight:400">${log.note}</div>`:''} ${log.images&&log.images.length?`<div style="display:flex;gap:6px;flex-wrap:wrap;margin-bottom:10px">${log.images.map(img=>`<img src="${img.data}" style="width:58px;height:58px;object-fit:cover;border-radius:8px;border:1px solid rgba(180,200,240,0.3)">`).join('')}</div>`:''} ${log.insight?`<div class="insight-card"><div class="insight-label">☁️ Coach Cloudy</div><div style="font-size:12px;color:var(--text);line-height:1.75;font-weight:400">${log.insight}</div>${log.matchedQuote?`<div style="margin-top:10px;padding-top:8px;border-top:1px solid rgba(180,200,240,0.2)"><div style="font-size:8px;color:#7060a8;letter-spacing:2px;font-family:var(--mono);margin-bottom:3px">✦ Spark</div><div style="font-size:11px;color:var(--text-m);font-style:italic">「${log.matchedQuote}」</div></div>`:''}</div>`:`<div style="font-size:9px;color:var(--text-d);font-family:var(--mono)">Generating insight...</div>`}</div>`}
function showLogForm(){const today=new Date().toISOString().split('T')[0];selectedMood='steady';uploadedImages=[];document.getElementById('log-form-wrap').innerHTML=`<div class="card pglow" style="margin-bottom:14px"><div class="frow" style="margin-bottom:10px"><div class="ff"><label class="fl">Date</label><input class="fi" type="date" id="lf-date" value="${today}"></div><div class="ff"><label class="fl">Activity</label><select class="fi" id="lf-type">${ACT_TYPES.map(t=>`<option>${t}</option>`).join('')}</select></div></div><div class="ff"><label class="fl">Feeling</label><div style="display:flex;gap:6px;flex-wrap:wrap" id="mood-sel">${EMOJI_MOODS.map(m=>`<button class="mood-btn${m.id==='steady'?' sel':''}" id="mood-${m.id}" onclick="selectMood('${m.id}')">${m.emoji} ${m.label}</button>`).join('')}</div></div><div class="ff" style="margin-top:8px"><label class="fl">心得 / 描述</label><textarea class="fi" id="lf-note" placeholder="今天練了什麼？感覺怎樣？沒有課表照片就在這裡描述..."></textarea></div><div class="ff" style="margin-top:4px"><label class="fl">截圖上傳（選填）</label><div style="display:flex;gap:6px;flex-wrap:wrap;margin-bottom:8px" id="upload-prev"></div><input type="file" accept="image/*" multiple id="file-upload-input" style="display:none" onchange="previewImages(this)">
<button type="button" onclick="document.getElementById('file-upload-input').click()" style="width:100%;display:flex;align-items:center;gap:8px;padding:9px 14px;background:rgba(235,245,255,0.55);border:1px dashed rgba(160,185,230,0.30);border-radius:12px;cursor:pointer;font-family:inherit;text-align:left">
  <span style="font-size:16px">📸</span>
  <span style="font-size:12px;color:var(--text-d)">Garmin · InBody · 課表截圖</span>
</button></div><div class="brow" style="margin-top:12px"><button class="bpri" onclick="submitLog()" id="log-submit-btn">送出 · Coach Cloudy ☁️</button><button class="bsec" onclick="cancelLogForm()">取消</button></div><div id="log-coach-resp" style="display:none;margin-top:12px"></div></div>`;document.getElementById('log-form-wrap').scrollIntoView({behavior:'smooth',block:'start'})}
function selectMood(id){selectedMood=id;EMOJI_MOODS.forEach(m=>{const btn=document.getElementById(`mood-${m.id}`);if(!btn)return;if(m.id===id){btn.classList.add('sel');btn.style.borderColor=m.color;btn.style.background=`${m.color}18`}else{btn.classList.remove('sel');btn.style.borderColor='';btn.style.background=''}})}
function previewImages(input){uploadedImages=[];const prev=document.getElementById('upload-prev');prev.innerHTML='';Array.from(input.files).forEach(file=>{const reader=new FileReader();reader.onload=e=>{uploadedImages.push({type:file.type,data:e.target.result});prev.innerHTML+=`<img src="${e.target.result}" style="width:58px;height:58px;object-fit:cover;border-radius:8px;border:1px solid rgba(180,200,240,0.3)">`};reader.readAsDataURL(file)})}
async function submitLog(){const date=document.getElementById('lf-date').value,actType=document.getElementById('lf-type').value,note=document.getElementById('lf-note').value.trim(),mood=EMOJI_MOODS.find(m=>m.id===selectedMood);if(!note&&uploadedImages.length===0)return;const btn=document.getElementById('log-submit-btn');btn.disabled=true;btn.textContent='Coach Cloudy 讀取中...';const respEl=document.getElementById('log-coach-resp');respEl.style.display='block';respEl.innerHTML=`<div class="insight-card"><div class="insight-label">☁️ Coach Cloudy</div><div style="display:flex;gap:4px;padding:2px"><div class="tdot"></div><div class="tdot"></div><div class="tdot"></div></div></div>`;const rl=LS.get('cc_logs',[]).slice(0,5).map(l=>`${l.date} ${l.actType} [${l.mood}]: ${l.note||'(圖片)'}`).join('\n');const sys=buildSys()+`\n針對這次訓練給洞察——讀懂她說的再回應，可以指出問題給建議。120字以內。\n最近記錄：\n${rl||'無'}`;try{const mc=[];uploadedImages.forEach(img=>mc.push({type:'image',source:{type:'base64',media_type:img.type||'image/jpeg',data:img.data.split(',')[1]}}));mc.push({type:'text',text:`日期：${date}\n活動：${actType}\n情緒：${mood.emoji}${mood.label}\n心得：${note||'(只有截圖)'}\n給洞察。`});const res=await fetch('https://api.anthropic.com/v1/messages',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({model:'claude-sonnet-4-20250514',max_tokens:1000,system:sys,messages:[{role:'user',content:mc}]})});const data=await res.json();const insight=data.content?.find(b=>b.type==='text')?.text||'記錄下來了。';let matchedQuote=null;if(quotes.length){try{const qr=await fetch('https://api.anthropic.com/v1/messages',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({model:'claude-sonnet-4-20250514',max_tokens:1000,system:'根據情緒和內容，從金句庫選最契合的一句，只回傳該句內容。',messages:[{role:'user',content:`情緒：${selectedMood}，描述：${note}\n金句庫：${quotes.map((q,i)=>`${i+1}.「${q.text}」`).join('\n')}\n選最配的。`}]})});const qd=await qr.json();const matched=qd.content?.find(b=>b.type==='text')?.text||'';const found=quotes.find(q=>matched.includes(q.text.substring(0,8)));if(found)matchedQuote=found.text;}catch{}}respEl.innerHTML=`<div class="insight-card"><div class="insight-label">☁️ Coach Cloudy</div><div style="font-size:13px;color:var(--text);line-height:1.8;font-weight:400">${insight}</div>${matchedQuote?`<div style="margin-top:10px;padding-top:8px;border-top:1px solid rgba(180,200,240,0.2)"><div style="font-size:8px;color:#7060a8;letter-spacing:2px;font-family:var(--mono);margin-bottom:3px">✦ Spark</div><div style="font-size:12px;color:var(--text-m);font-style:italic">「${matchedQuote}」</div></div>`:''}</div>`;const logs=LS.get('cc_logs',[]);logs.unshift({id:Date.now(),date,actType,mood:selectedMood,note,images:uploadedImages.map(i=>({data:i.data})),insight,matchedQuote});LS.set('cc_logs',logs);btn.textContent='✓ Logged';setTimeout(()=>{cancelLogForm();renderLogPage()},2000);}catch{respEl.innerHTML=`<div style="font-size:12px;color:var(--text-m);padding:8px">網路問題，記錄已存。</div>`;btn.disabled=false;btn.textContent='送出 · Coach Cloudy ☁️'}}
function deleteLog(id){LS.set('cc_logs',LS.get('cc_logs',[]).filter(l=>l.id!==id));renderLogPage()}
function cancelLogForm(){document.getElementById('log-form-wrap').innerHTML='';selectedMood='steady';uploadedImages=[]}
async function analyzePattern(){const btn=document.querySelector('[onclick="analyzePattern()"]');if(btn){btn.disabled=true;btn.textContent='Analyzing...'}const logs=LS.get('cc_logs',[]);const summary=logs.map(l=>`${l.date}|${l.actType}|${l.mood}|${l.note||'(圖片)'}`).join('\n');try{const msg=await callAI([{role:'user',content:`訓練記錄：\n${summary}\n分析我的訓練模式。`}],buildSys()+'\n從記錄裡找具體規律，條列2-3個具體發現，150字以內。');document.getElementById('pattern-result').innerHTML=`<div class="card pglow" style="margin-top:10px"><div style="display:flex;align-items:center;gap:8px;margin-bottom:10px"><div class="cavatar" style="width:28px;height:28px;font-size:13px">☁️</div><div style="font-size:8px;color:#7060a8;letter-spacing:3px;font-family:var(--mono);font-weight:600">PATTERN ANALYSIS</div></div><div style="font-size:13px;color:var(--text);line-height:1.85;white-space:pre-wrap;font-weight:400">${msg}</div></div>`;}catch{document.getElementById('pattern-result').innerHTML=''}if(btn){btn.disabled=false;btn.textContent='🔍 Analyze My Pattern'}}
function renderRaceList(){document.getElementById('page-race').innerHTML=`<div class="slabel sb" style="margin-bottom:14px">Race Hub</div>${Object.values(RACE_DATA).map(r=>`<div class="race-item ${r.isHyrox?'hyrox-panel':''}" style="border-color:${r.isHyrox?'#1c1c00':'rgba(255,255,255,0.80)'};margin-bottom:10px" onclick="openRaceDetail('${r.id}')"><span class="ri-icon">${r.icon}</span><div class="ri-info"><div class="ri-name" style="${r.isHyrox?'color:#FFE600':''}">${r.name}</div><div class="ri-target" style="${r.isHyrox?'color:rgba(255,230,0,0.62)':''}">${r.desc}</div></div><span class="ri-chev" style="color:${r.isHyrox?'rgba(255,230,0,0.55)':'var(--text-d)'}">›</span></div>`).join('')}`}
function openRaceDetail(id){const r=RACE_DATA[id];document.getElementById('detail-title').textContent=r.name;document.getElementById('detail-icon').textContent=r.icon;const H=r.isHyrox;const cp=H?'background:#050500;border-color:#1a1a00':'';const saved=LS.get(`race_records_${id}`,[]);let html=`<div class="card ${H?'hyrox-panel':''}" style="${cp};margin-bottom:16px"><div style="font-size:11px;color:${H?'rgba(255,230,0,0.78)':'var(--text-m)'};margin-bottom:6px;font-family:var(--mono);font-weight:400">${r.desc}</div><div style="font-size:13px;font-weight:600;color:${H?'#FFE600':'#4870a8'}">${r.target}</div></div>`;html+=`<div style="font-size:8px;color:${H?'#FFE600':'#4870a8'};letter-spacing:3px;font-family:var(--mono);font-weight:600;margin-bottom:10px">PERSONAL RECORDS</div>`;if(saved.length){saved.forEach(rec=>{html+=`<div class="card ${H?'hyrox-panel':''}" style="${cp};margin-bottom:8px"><div style="font-size:8px;color:${H?'rgba(255,230,0,0.52)':'var(--text-d)'};font-family:var(--mono);margin-bottom:8px">${rec.date}</div><div style="display:grid;grid-template-columns:repeat(3,1fr);gap:8px">${rec.stats.map(s=>`<div class="rstat" style="${H?'background:rgba(255,230,0,0.07)':''}"><div class="rstat-val" style="${H?'color:#FFE600':''}">${s.val}</div><div class="rstat-label" style="${H?'color:rgba(255,230,0,0.5)':''}">${s.label}</div></div>`).join('')}</div></div>`})}else{html+=`<div style="font-size:12px;color:var(--text-d);padding:12px 0;font-family:var(--mono)">No records yet.</div>`}html+=`<button class="btn-add-record" style="${H?'border-color:rgba(255,230,0,0.28);color:rgba(255,230,0,0.75)':''}" onclick="addRecord('${id}')">+ Add Race Record</button><div id="record-form-${id}"></div>`;html+=`<div style="font-size:8px;color:${H?'#FFE600':'#4870a8'};letter-spacing:3px;font-family:var(--mono);font-weight:600;margin:16px 0 10px">TRAINING MOVES</div>`;html+=r.training.map(t=>`<div class="training-item ${H?'hyrox-panel':''}" style="${cp}"><span style="font-size:16px">${t.icon}</span><span class="training-name" style="${H?'color:#f0f0e0':''}">${t.name}</span><span class="training-tag ${t.tag}">${t.tl}</span></div>`).join('');document.getElementById('race-detail-content').innerHTML=html;document.querySelectorAll('.page').forEach(p=>p.classList.remove('active'));document.getElementById('page-race-detail').classList.add('active')}
function addRecord(id){const H=id==='hyrox';const cs=H?'background:#050500;border-color:#1a1a00':'';const fs=H?'background:rgba(255,230,0,0.05);border-color:rgba(255,230,0,0.2);color:#FFE600':'';let fields='';if(id==='hyrox')fields=`<div class="ff"><label class="fl" style="${H?'color:rgba(255,230,0,0.55)':''}">總時間</label><input class="fi" id="rf-total" placeholder="1:12:30" style="${fs}"></div><div class="frow"><div class="ff"><label class="fl" style="${H?'color:rgba(255,230,0,0.55)':''}">跑步</label><input class="fi" id="rf-run" placeholder="48:00" style="${fs}"></div><div class="ff"><label class="fl" style="${H?'color:rgba(255,230,0,0.55)':''}">功能站</label><input class="fi" id="rf-station" placeholder="24:30" style="${fs}"></div></div>`;else if(id==='running')fields=`<div class="frow"><div class="ff"><label class="fl">距離</label><input class="fi" id="rf-dist" placeholder="21.1 km"></div><div class="ff"><label class="fl">完賽時間</label><input class="fi" id="rf-total" placeholder="1:48:22"></div></div><div class="frow"><div class="ff"><label class="fl">配速</label><input class="fi" id="rf-pace" placeholder="5:08/km"></div><div class="ff"><label class="fl">平均心率</label><input class="fi" id="rf-hr" placeholder="158 bpm"></div></div>`;else fields=`<div class="frow"><div class="ff"><label class="fl">騎車</label><input class="fi" id="rf-bike" placeholder="1:10:00"></div><div class="ff"><label class="fl">跑步</label><input class="fi" id="rf-run" placeholder="45:00"></div></div><div class="frow"><div class="ff"><label class="fl">換項 T1</label><input class="fi" id="rf-t1" placeholder="1:30"></div><div class="ff"><label class="fl">總時間</label><input class="fi" id="rf-total" placeholder="1:58:30"></div></div>`;document.getElementById(`record-form-${id}`).innerHTML=`<div class="card" style="${cs};margin-top:8px"><div class="ff"><label class="fl" style="${H?'color:rgba(255,230,0,0.55)':''}">Date</label><input class="fi" id="rf-date" type="date" value="${new Date().toISOString().split('T')[0]}" style="${fs}"></div>${fields}<div class="brow"><button class="bpri" onclick="saveRecord('${id}')">Save</button><button class="bsec" onclick="document.getElementById('record-form-${id}').innerHTML=''">Cancel</button></div></div>`}
function saveRecord(id){const date=document.getElementById('rf-date')?.value||new Date().toISOString().split('T')[0];let stats=[];if(id==='hyrox')stats=[{label:'Total',val:document.getElementById('rf-total')?.value||'—'},{label:'Run',val:document.getElementById('rf-run')?.value||'—'},{label:'Stations',val:document.getElementById('rf-station')?.value||'—'}];else if(id==='running')stats=[{label:'Time',val:document.getElementById('rf-total')?.value||'—'},{label:'Pace',val:document.getElementById('rf-pace')?.value||'—'},{label:'HR',val:document.getElementById('rf-hr')?.value||'—'}];else stats=[{label:'Bike',val:document.getElementById('rf-bike')?.value||'—'},{label:'Run',val:document.getElementById('rf-run')?.value||'—'},{label:'Total',val:document.getElementById('rf-total')?.value||'—'}];const records=LS.get(`race_records_${id}`,[]);records.unshift({date,stats});LS.set(`race_records_${id}`,records);openRaceDetail(id)}
function backToRace(){document.querySelectorAll('.page').forEach(p=>p.classList.remove('active'));document.getElementById('page-race').classList.add('active')}
function renderGoals(){renderGoalList('body','body-goals-list');renderGoalList('race','race-goals-list');renderQuoteList()}
function renderGoalList(type,elId){const el=document.getElementById(elId);if(!el)return;if(!goals[type].length){el.innerHTML=`<div style="color:var(--text-d);font-size:12px;font-family:var(--mono);margin-bottom:10px">No goals yet.</div>`;return}el.innerHTML=goals[type].map(g=>{const d=daysTo(g.deadline);return`<div class="card" style="margin-bottom:10px"><div class="gci"><div class="gicon">${g.icon}</div><div class="ginfo"><div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:4px"><span class="gname">${g.label}</span>${d!==null?`<span class="gdays ${d<30?'urgent':'ok'}">${d}d left</span>`:''}</div><div class="gvals"><span>Now <strong style="color:#4870a8">${g.current||'—'}${g.unit}</strong></span><span style="color:var(--text-d)">→</span><span>Goal <strong style="color:#7060a8">${g.target}${g.unit}</strong></span></div></div><div class="gacts"><button class="gact" style="color:#4870a8" onclick="editGoal('${type}',${g.id})">✎</button><button class="gact" style="color:rgba(150,60,80,0.5)" onclick="deleteGoal('${type}',${g.id})">×</button></div></div></div>`}).join('')}
function renderQuoteList(){const el=document.getElementById('quotes-list');if(!el)return;el.innerHTML=quotes.map(q=>`<div class="card" style="margin-bottom:8px"><div class="qci"><div style="flex:1"><div class="qt">「${q.text}」</div>${q.source?`<div class="qs">— ${q.source}</div>`:''}</div><button class="qdel" onclick="deleteQuote(${q.id})">×</button></div></div>`).join('')}
function showGoalForm(g){g=g||{label:'',current:'',target:'',unit:'',deadline:'',icon:'🎯',type:'body'};document.getElementById('goal-form-wrap').innerHTML=`<div class="card pglow"><div class="frow"><div class="ff"><label class="fl">Type</label><select class="fi" id="gf-type"><option value="body" ${g.type==='body'?'selected':''}>Body Goal</option><option value="race" ${g.type==='race'?'selected':''}>Race Goal</option></select></div><div class="ff"><label class="fl">Icon</label><input class="fi" id="gf-icon" value="${g.icon}" style="text-align:center;font-size:18px"></div></div><div class="ff"><label class="fl">Name</label><input class="fi" id="gf-label" value="${g.label}" placeholder="e.g. Weight"></div><div class="frow"><div class="ff"><label class="fl">Current</label><input class="fi" id="gf-cur" value="${g.current||''}" placeholder="52.8"></div><div class="ff"><label class="fl">Target</label><input class="fi" id="gf-tgt" value="${g.target||''}" placeholder="48"></div></div><div class="frow"><div class="ff"><label class="fl">Unit</label><input class="fi" id="gf-unit" value="${g.unit||''}" placeholder="kg"></div><div class="ff"><label class="fl">Deadline</label><input class="fi" id="gf-ddl" type="date" value="${g.deadline||''}"></div></div><div class="brow"><button class="bpri" onclick="saveGoal(${g.id||'null'})">Save</button><button class="bsec" onclick="cancelGoalForm()">Cancel</button></div></div>`;document.getElementById('goal-form-wrap').scrollIntoView({behavior:'smooth',block:'nearest'})}
function editGoal(type,id){const g=goals[type].find(x=>x.id===id);if(g)showGoalForm({...g,type})}
function saveGoal(eid){const type=document.getElementById('gf-type').value;const g={id:eid||Date.now(),type,icon:document.getElementById('gf-icon').value||'🎯',label:document.getElementById('gf-label').value,current:document.getElementById('gf-cur').value,target:document.getElementById('gf-tgt').value,unit:document.getElementById('gf-unit').value,deadline:document.getElementById('gf-ddl').value};if(!g.label)return;if(eid){goals.body=goals.body.filter(x=>x.id!==eid);goals.race=goals.race.filter(x=>x.id!==eid)}goals[type].push(g);LS.set('cc_goals',goals);cancelGoalForm();renderGoals();renderDashboard()}
function deleteGoal(type,id){goals[type]=goals[type].filter(g=>g.id!==id);LS.set('cc_goals',goals);renderGoals();renderDashboard()}
function cancelGoalForm(){document.getElementById('goal-form-wrap').innerHTML=''}
function showQuoteForm(){document.getElementById('quote-form-wrap').innerHTML=`<div class="card pglow"><div class="ff"><label class="fl">Quote</label><textarea class="fi" id="qf-text" placeholder="Something that fires you up..."></textarea></div><div class="ff"><label class="fl">Source</label><input class="fi" id="qf-src" placeholder="Author, book..."></div><div class="brow"><button class="bpri" onclick="saveQuote()">Add</button><button class="bsec" onclick="cancelQuoteForm()">Cancel</button></div></div>`}
function saveQuote(){const t=document.getElementById('qf-text').value.trim();if(!t)return;quotes.push({id:Date.now(),text:t,source:document.getElementById('qf-src').value});LS.set('cc_quotes',quotes);cancelQuoteForm();renderQuoteList()}
function deleteQuote(id){quotes=quotes.filter(q=>q.id!==id);LS.set('cc_quotes',quotes);renderQuoteList()}
function cancelQuoteForm(){document.getElementById('quote-form-wrap').innerHTML=''}
function switchTab(tab,btn){document.querySelectorAll('.page').forEach(p=>p.classList.remove('active'));document.querySelectorAll('.nb').forEach(b=>b.classList.remove('active'));document.getElementById('page-'+tab).classList.add('active');btn.classList.add('active');if(tab==='goals')renderGoals();if(tab==='race')renderRaceList();if(tab==='record')renderLogPage();if(tab==='profile')renderProfilePage()}
function openCoach(){document.getElementById('coach-overlay').classList.add('open');if(!chatHistory.length)initCoach()}
function closeCoach(){document.getElementById('coach-overlay').classList.remove('open')}
async function initCoach(){addTyping();try{const msg=await callAI([{role:'user',content:'給我一句開場激勵，直接說，不超過80字。'}],buildSys());removeTyping();const r=msg||'你差4.8公斤和無數個訓練。今天，先做今天的那個。';chatHistory.push({role:'assistant',content:r});addMsg('ai',r)}catch{removeTyping();const r='目標清楚，路線清楚。剩下的只有執行。';chatHistory.push({role:'assistant',content:r});addMsg('ai',r)}}
async function sendMsg(){const input=document.getElementById('chat-in');const text=input.value.trim();if(!text||coachLoading)return;input.value='';chatHistory.push({role:'user',content:text});addMsg('user',text);coachLoading=true;document.getElementById('chat-send').disabled=true;addTyping();try{const r=await callAI(chatHistory,buildSys());removeTyping();const msg=r||'繼續。';chatHistory.push({role:'assistant',content:msg});addMsg('ai',msg)}catch{removeTyping();addMsg('ai','網路問題，但訓練不能停。')}coachLoading=false;document.getElementById('chat-send').disabled=false}
function addMsg(role,text){const msgs=document.getElementById('chat-msgs');const row=document.createElement('div');row.className=`mrow ${role}`;row.innerHTML=role==='ai'?`<div class="mavatar">☁️</div><div class="mbubble ai">${text}</div>`:`<div class="mbubble user">${text}</div>`;msgs.appendChild(row);msgs.scrollTop=msgs.scrollHeight}
function addTyping(){const msgs=document.getElementById('chat-msgs');const row=document.createElement('div');row.className='mrow ai';row.id='typing-row';row.innerHTML='<div class="mavatar">☁️</div><div class="mbubble ai"><div class="tdots"><div class="tdot"></div><div class="tdot"></div><div class="tdot"></div></div></div>';msgs.appendChild(row);msgs.scrollTop=msgs.scrollHeight}
function removeTyping(){const el=document.getElementById('typing-row');if(el)el.remove()}

// ══ PROFILE MODULE ════════════════════════════════════════════
// ══ PROFILE MODULE ════════════════════════════════════════════

const DEF_PROFILE = {
  // 命理
  birthday: '1984-01-09',
  birthtime: '14:34',
  birthplace: '台灣台北',
  zodiac: '魔羯座',
  mbti: 'INTJ',

  // 星盤
  astro: {
    sun:     { sign:'魔羯座', deg:'18°11\'', house:'8宮' },
    moon:    { sign:'雙魚座', deg:'24°42\'', house:'10宮' },
    rising:  { sign:'雙子座', deg:'10°33\'', house:'1宮' },
    mercury: { sign:'魔羯座', deg:'0°37\'',  house:'7宮', retro:true },
    venus:   { sign:'射手座', deg:'9°53\'',  house:'6宮' },
    mars:    { sign:'天秤座', deg:'29°02\'', house:'5宮' },
    jupiter: { sign:'射手座', deg:'27°45\'', house:'7宮' },
    saturn:  { sign:'天蠍座', deg:'14°36\'', house:'6宮' },
    uranus:  { sign:'射手座', deg:'11°35\'', house:'7宮' },
    neptune: { sign:'射手座', deg:'29°39\'', house:'7宮' },
    pluto:   { sign:'天蠍座', deg:'1°56\'',  house:'5宮' },
    mc:      { sign:'水瓶座', deg:'25°48\'', house:'10宮' },
  },

  // 人類圖
  humanDesign: {
    type: '顯示者',
    profile: '4/6',
    definition: '二分人',
    authority: '情緒型',
    strategy: '告知',
    notSelf: '憤怒',
    channels: ['57-10（自愛/形象）', '35-36（情緒激勵者）'],
    incarnationCross: '右角度交叉之滲透4（54/53｜57/51）',
  },

  // 八字
  bazi: {
    year: '癸亥', month: '癸丑', day: '癸酉', hour: '未時',
    note: '四柱全癸，水極旺',
  },

  // Coach 自訂
  coachPersonality: '帥氣霸道，說話簡短有力，每句都有份量。直接指出問題，不廢話，但讓人感覺被懂、不是被罵。嚴格中偶爾藏一句暖的，不濫情但有溫度。像那種你知道他是真心為你好的教練，不是冷漠的機器。繁體中文，120字以內，說話像人不像報告。',
  coachFocus: '均衡',  // 體態 / 跑步 / 備賽 / 恢復 / 均衡
  coachTone: 3,        // 1=溫柔 5=嚴格
};

function getProfile() {
  try {
    const saved = localStorage.getItem('cc_profile');
    return saved ? { ...DEF_PROFILE, ...JSON.parse(saved) } : DEF_PROFILE;
  } catch { return DEF_PROFILE; }
}
function saveProfile(p) {
  try { localStorage.setItem('cc_profile', JSON.stringify(p)); } catch {}
}

// ── Build full system prompt from profile ────────────────────
function buildSysFromProfile() {
  const p = getProfile();
  const bg = goals.body.map(g => `${g.label}: ${g.current}${g.unit}→${g.target}${g.unit}`).join('; ');
  const rg = goals.race.map(g => `${g.label}: target ${g.target}`).join('; ');
  const qs = quotes.map(q => `「${q.text}」——${q.source}`).join('；');

  const focusMap = { '體態':'以體態雕塑和減脂為優先', '跑步':'以跑步訓練和配速提升為優先', '備賽':'以比賽準備和峰值體能為優先', '恢復':'以恢復和身體修復為優先', '均衡':'均衡考量所有面向' };
  const toneMap = { 1:'非常溫柔體貼', 2:'溫柔中帶嚴格', 3:'嚴格中帶溫度', 4:'嚴格直接', 5:'非常嚴格毫不留情' };

  return `你是 Cathy 的私人綜合教練 Coach Cloudy，同時扮演三個嚴格角色：
【重量訓練教練】體能評估、訓練菜單、動作邏輯、訓練量控管
【跑步教練】半馬破100分鐘、越野賽出色表現、週期化訓練、比賽策略
【營養師】保肌減脂、可長期執行、顧氣色不顯老

━━ Cathy 的完整資料 ━━
生日：${p.birthday} ${p.birthtime}，${p.birthplace}
星座：太陽${p.astro.sun.sign} / 月亮${p.astro.moon.sign} / 上升${p.astro.rising.sign}
八字：${p.bazi.year}年 ${p.bazi.month}月 ${p.bazi.day}日 ${p.bazi.hour}（${p.bazi.note}）
人類圖：${p.humanDesign.type} ${p.humanDesign.profile} ${p.humanDesign.definition}，${p.humanDesign.authority}權威，策略「${p.humanDesign.strategy}」，通道：${p.humanDesign.channels.join('、')}
MBTI：${p.mbti}

體態目標：${bg}
比賽目標：${rg}
額外目標：Hyrox個人+混雙拿得出手、體操pull up、倒立
現況：D-type體型，體脂20%，骨骼肌23.2kg，InBody 80分
本週重點：${focusMap[p.coachFocus] || focusMap['均衡']}

她的金句：${qs}

━━ Coach 回應風格 ━━
${p.coachPersonality}
語氣強度：${toneMap[p.coachTone] || toneMap[3]}`;
}

// ── Weekly Orbit: real AI with astrology ─────────────────────
async function generateOrbit() {
  const el = document.getElementById('orbit-content');
  if (!el) return;
  el.innerHTML = `<div style="display:flex;gap:4px;padding:8px 0"><div class="tdot"></div><div class="tdot"></div><div class="tdot"></div></div>`;

  const p = getProfile();
  const today = new Date();
  const weekNum = Math.ceil(today.getDate() / 7);
  const month = today.getMonth() + 1;

  const sys = `你是融合占星、八字、人類圖、MBTI的命理顧問，為 Cathy 提供本週個人化運勢。
請用溫暖但精準的語氣，針對她的本命盤給出這週實際有用的指引。
不要泛泛而談，要結合她的具體星盤特質說。
輸出格式：四個區塊，每塊2-3句，結尾給一個本週行動建議。
繁體中文。`;

  const prompt = `Cathy 的命理資料：
太陽魔羯8宮 / 月亮雙魚10宮 / 上升雙子1宮 / 水星魔羯逆行7宮 / 金星射手6宮 / 火星天秤5宮 / 木星射手7宮 / 土星天蠍6宮
八字：癸亥癸丑癸酉未時（四柱全癸水旺）
人類圖：顯示者4/6，情緒權威，通道57-10和35-36
MBTI：INTJ

現在是 ${today.getFullYear()}年${month}月第${weekNum}週。
請給她這週的個人化運勢，分四個區塊：
1. ✨ 體態 · 美貌（結合星盤談身體能量和外在狀態）
2. ⚡ 運動表現（結合八字流月談訓練時機和體能峰值）
3. 🔮 人類圖能量（這週她的能量場適合什麼、要告知什麼）
4. 💎 INTJ 心智（這週決策和直覺的建議）
最後給「✦ 本週行動」一句具體建議。`;

  try {
    const msg = await callAI([{ role: 'user', content: prompt }], sys);
    // Parse and render
    const sections = [];
    const lines = msg.split('\n').filter(l => l.trim());
    let current = null;
    lines.forEach(line => {
      if (line.match(/^[1-4]\.|✨|⚡|🔮|💎/)) {
        if (current) sections.push(current);
        current = { label: line.replace(/^[1-4]\.\s*/, ''), text: '' };
      } else if (line.startsWith('✦') || line.startsWith('本週行動')) {
        if (current) sections.push(current);
        current = { label: '✦ 本週行動', text: line.replace(/✦\s*本週行動[:：]?\s*/, ''), isAction: true };
      } else if (current) {
        current.text += (current.text ? ' ' : '') + line.trim();
      }
    });
    if (current) sections.push(current);

    el.innerHTML = sections.map(s => s.isAction
      ? `<div class="orbit-action"><div class="orbit-act-label">${s.label}</div><div class="orbit-act-text">${s.text}</div></div>`
      : `<div class="orbit-sec"><div class="orbit-sec-label">${s.label}</div><div class="orbit-sec-text">${s.text}</div></div>`
    ).join('') || `<div style="font-size:12px;color:var(--text-m);line-height:1.78">${msg}</div>`;
  } catch {
    // fallback to static
    const f = FP[Math.floor(Math.random() * FP.length)];
    el.innerHTML = [['✨ 體態 · 美貌', f.beauty], ['⚡ 運動表現', f.sport], ['🔮 比賽運勢', f.race], ['💎 身體狀態', f.body]]
      .map(([l, t]) => `<div class="orbit-sec"><div class="orbit-sec-label">${l}</div><div class="orbit-sec-text">${t}</div></div>`).join('')
      + `<div class="orbit-action"><div class="orbit-act-label">✦ 本週行動</div><div class="orbit-act-text">${f.action}</div></div>`;
  }
}

// ── Profile Page ─────────────────────────────────────────────
function renderProfilePage() {
  const p = getProfile();
  const toneLabels = ['', '溫柔', '溫柔嚴格', '嚴格溫度', '嚴格直接', '非常嚴格'];
  const focusOptions = ['體態', '跑步', '備賽', '恢復', '均衡'];

  document.getElementById('page-profile').innerHTML = `
    <div class="slabel si" style="margin-bottom:14px">個人檔案</div>

    <!-- 命理資料 -->
    <div class="card pglow" style="margin-bottom:12px">
      <div class="slabel sv" style="margin-bottom:10px">🔮 命理資料</div>
      <div class="prof-grid">
        <div class="prof-item"><div class="prof-label">生日</div><div class="prof-val">${p.birthday} ${p.birthtime}</div></div>
        <div class="prof-item"><div class="prof-label">出生地</div><div class="prof-val">${p.birthplace}</div></div>
        <div class="prof-item"><div class="prof-label">太陽</div><div class="prof-val">${p.astro.sun.sign} ${p.astro.sun.house}</div></div>
        <div class="prof-item"><div class="prof-label">月亮</div><div class="prof-val">${p.astro.moon.sign} ${p.astro.moon.house}</div></div>
        <div class="prof-item"><div class="prof-label">上升</div><div class="prof-val">${p.astro.rising.sign}</div></div>
        <div class="prof-item"><div class="prof-label">八字</div><div class="prof-val">${p.bazi.year} ${p.bazi.month} ${p.bazi.day} ${p.bazi.hour}</div></div>
        <div class="prof-item"><div class="prof-label">人類圖</div><div class="prof-val">${p.humanDesign.type} ${p.humanDesign.profile}</div></div>
        <div class="prof-item"><div class="prof-label">權威</div><div class="prof-val">${p.humanDesign.authority}</div></div>
        <div class="prof-item"><div class="prof-label">MBTI</div><div class="prof-val">${p.mbti}</div></div>
        <div class="prof-item"><div class="prof-label">通道</div><div class="prof-val">${p.humanDesign.channels.join(' / ')}</div></div>
      </div>
    </div>

    <!-- Coach 設定 -->
    <div class="card" style="margin-bottom:12px">
      <div class="slabel si" style="margin-bottom:10px">☁️ Coach 設定</div>

      <div class="ff">
        <label class="fl">本週訓練重點</label>
        <div style="display:flex;gap:6px;flex-wrap:wrap" id="focus-sel">
          ${focusOptions.map(f => `
            <button onclick="setFocus('${f}')" id="focus-${f}"
              class="mood-btn${p.coachFocus === f ? ' sel' : ''}"
              style="${p.coachFocus === f ? 'border-color:#7aaad8;background:rgba(122,170,216,0.15);color:var(--text)' : ''}">
              ${f}
            </button>`).join('')}
        </div>
      </div>

      <div class="ff" style="margin-top:12px">
        <label class="fl">語氣強度 — ${toneLabels[p.coachTone] || '嚴格溫度'}</label>
        <div style="display:flex;align-items:center;gap:10px">
          <span style="font-size:10px;color:var(--text-d);font-family:var(--mono)">溫柔</span>
          <input type="range" min="1" max="5" value="${p.coachTone}" id="tone-slider"
            style="flex:1;accent-color:#7aaad8"
            oninput="updateToneLabel(this.value)"
            onchange="saveTone(this.value)">
          <span style="font-size:10px;color:var(--text-d);font-family:var(--mono)">嚴格</span>
        </div>
        <div style="text-align:center;font-size:10px;color:#4870a8;font-family:var(--mono);margin-top:4px" id="tone-label">${toneLabels[p.coachTone]}</div>
      </div>

      <div class="ff" style="margin-top:12px">
        <label class="fl">Coach 人設 Prompt</label>
        <textarea class="fi" id="coach-persona" style="min-height:100px;font-size:12px">${p.coachPersonality}</textarea>
        <button class="bpri" style="margin-top:8px;width:100%" onclick="savePersona()">儲存 Coach 設定</button>
      </div>
    </div>

    <!-- API Key -->
    <div class="card" style="margin-bottom:12px">
      <div class="slabel si" style="margin-bottom:10px">🔑 API Key</div>
      <div style="font-size:11px;color:var(--text-m);margin-bottom:10px;line-height:1.6">
        Key 只存在你的手機，不會上傳到任何地方。<br>
        取得：console.anthropic.com → API Keys
      </div>
      <input class="fi" id="prof-apikey" type="password"
        placeholder="${getApiKey() ? '已設定（點擊更新）' : 'sk-ant-api03-...'}"
        style="font-family:var(--mono);font-size:12px;margin-bottom:8px">
      <button class="bpri" style="width:100%" onclick="saveApiKeyFromProfile()">儲存 API Key</button>
    </div>

    <div style="height:20px"></div>
  `;
}

function setFocus(f) {
  const p = getProfile();
  p.coachFocus = f;
  saveProfile(p);
  // Update buttons
  ['體態','跑步','備賽','恢復','均衡'].forEach(opt => {
    const btn = document.getElementById(`focus-${opt}`);
    if (!btn) return;
    if (opt === f) {
      btn.classList.add('sel');
      btn.style.borderColor = '#7aaad8';
      btn.style.background = 'rgba(122,170,216,0.15)';
      btn.style.color = 'var(--text)';
    } else {
      btn.classList.remove('sel');
      btn.style.borderColor = '';
      btn.style.background = '';
      btn.style.color = '';
    }
  });
}

const toneLabels2 = ['', '溫柔', '溫柔嚴格', '嚴格溫度', '嚴格直接', '非常嚴格'];
function updateToneLabel(v) {
  const el = document.getElementById('tone-label');
  if (el) el.textContent = toneLabels2[v] || '';
}
function saveTone(v) {
  const p = getProfile();
  p.coachTone = parseInt(v);
  saveProfile(p);
}
function savePersona() {
  const p = getProfile();
  const val = document.getElementById('coach-persona')?.value?.trim();
  if (val) { p.coachPersonality = val; saveProfile(p); }
  // Show confirmation
  const btn = document.querySelector('[onclick="savePersona()"]');
  if (btn) { btn.textContent = '✓ 已儲存'; setTimeout(() => btn.textContent = '儲存 Coach 設定', 2000); }
}
function saveApiKeyFromProfile() {
  const val = document.getElementById('prof-apikey')?.value?.trim();
  if (val && val.startsWith('sk-ant-')) {
    localStorage.setItem('cc_apikey', val);
    const btn = document.querySelector('[onclick="saveApiKeyFromProfile()"]');
    if (btn) { btn.textContent = '✓ 已儲存'; setTimeout(() => btn.textContent = '儲存 API Key', 2000); }
  } else if (val) {
    document.getElementById('prof-apikey').style.borderColor = '#e05070';
  }
}


// ── API Key Management ─────────────────────────────────────────
function getApiKey(){return localStorage.getItem('cc_apikey')||''}

function saveApiKey(){
  const key=document.getElementById('apikey-input').value.trim();
  if(!key.startsWith('sk-ant-')){
    document.getElementById('apikey-input').style.borderColor='#e05070';
    document.getElementById('apikey-input').placeholder='格式不對，應該是 sk-ant-... 開頭';
    return;
  }
  localStorage.setItem('cc_apikey',key);
  document.getElementById('apikey-modal').classList.add('hidden');
  renderDashboard();
}

function skipApiKey(){
  document.getElementById('apikey-modal').classList.add('hidden');
  // Only render if not already rendered
  const homeEl = document.getElementById('coach-msg');
  if(homeEl && homeEl.textContent === 'Loading...') renderDashboard();
}

function showApiKeySettings(){
  const modal=document.getElementById('apikey-modal');
  const existing=getApiKey();
  if(existing){
    document.getElementById('apikey-input').placeholder=existing.slice(0,18)+'...（已儲存）';
  }
  modal.classList.remove('hidden');
}

function checkApiKeyOnLoad(){
  // Always render dashboard first
  renderDashboard();
  // Show modal on top if no key
  const key=getApiKey();
  if(!key){
    document.getElementById('apikey-modal').classList.remove('hidden');
  }
}

// Allow Enter key in API key input
document.getElementById('apikey-input').addEventListener('keydown',function(e){
  if(e.key==='Enter') saveApiKey();
});

// ── Goal panel breathing
let ps=false;
setInterval(()=>{ps=!ps;const gc=document.getElementById('goal-panel');if(gc){gc.style.boxShadow=ps?'0 12px 40px rgba(160,148,230,0.30),0 1px 0 rgba(255,255,255,0.98) inset':'0 8px 34px rgba(150,130,220,0.22),0 1px 0 rgba(255,255,255,0.98) inset';gc.style.transition='all 4s ease'}},4000);

// ── Auto-sync InBody data ─────────────────────────────────────
async function autoSyncInBody(images, insight, respEl){
  // Only attempt if actType suggests body measurement or insight mentions numbers
  const hasBodyData = insight && (
    insight.includes('體脂') || insight.includes('體重') ||
    insight.includes('肌肉') || insight.includes('InBody') ||
    insight.includes('kg') || insight.includes('%')
  );
  if(!hasBodyData && !images.length) return;

  try{
    const mc=[];
    images.forEach(img=>mc.push({type:'image',source:{type:'base64',media_type:img.type||'image/jpeg',data:img.data.split(',')[1]}}));
    mc.push({type:'text',text:`請從這張截圖中擷取身體數據。
如果是 InBody 報告，請回傳 JSON 格式：
{"type":"inbody","weight":數字或null,"bodyFat":數字或null,"muscle":數字或null,"visceralFat":數字或null,"inbodyScore":數字或null}
如果不是 InBody 報告，回傳：{"type":"none"}
只回傳 JSON，不要任何說明。`});

    const res=await fetch('https://api.anthropic.com/v1/messages',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({model:'claude-sonnet-4-20250514',max_tokens:200,messages:[{role:'user',content:mc}]})});
    const data=await res.json();
    const raw=data.content?.find(b=>b.type==='text')?.text||'';
    const clean=raw.replace(/```json|```/g,'').trim();
    const parsed=JSON.parse(clean);

    if(parsed.type==='inbody'){
      let updated=false;
      const bodyGoals=goals.body;

      if(parsed.weight!=null){
        const wg=bodyGoals.find(g=>g.label==='Weight');
        if(wg){wg.current=parsed.weight;updated=true}
      }
      if(parsed.bodyFat!=null){
        const bg=bodyGoals.find(g=>g.label==='Body Fat');
        if(bg){bg.current=parsed.bodyFat;updated=true}
      }
      if(parsed.muscle!=null){
        const mg=bodyGoals.find(g=>g.label==='Muscle');
        if(mg){mg.current=parsed.muscle;updated=true}
      }

      if(updated){
        LS.set('cc_goals',goals);
        // Show sync notification
        const syncNote=document.createElement('div');
        syncNote.style.cssText='margin-top:10px;padding:8px 12px;background:rgba(120,180,120,0.12);border:1px solid rgba(100,160,100,0.25);border-radius:10px;font-size:11px;color:#3a7a3a;font-family:var(--mono)';
        syncNote.innerHTML=`✦ InBody 數據已同步更新<br>${parsed.weight!=null?`體重 ${parsed.weight}kg · `:''}${parsed.bodyFat!=null?`體脂 ${parsed.bodyFat}% · `:''}${parsed.muscle!=null?`肌肉 ${parsed.muscle}kg`:''}`;
        if(respEl) respEl.appendChild(syncNote);
        // Refresh dashboard rings
        setTimeout(()=>renderDashboard(),500);
      }
    }
  }catch(e){
    // Silent fail — InBody sync is best-effort
    console.log('InBody sync skipped:',e.message);
  }
}

checkApiKeyOnLoad();
</script>
</body>
</html>
